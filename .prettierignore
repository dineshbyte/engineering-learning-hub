# build output + caches (docs/ is the Astro outDir; gitignored + generated)
dist
docs
.astro

# lockfile — machine-managed, never hand-formatted
pnpm-lock.yaml

# hand-maintained CSS source (dense, intentionally not Prettier-managed)
public/assets

# generated MDX lessons — bespoke JSX/frontmatter, not Prettier-managed (no MDX plugin)
src/content
src/components/figures
