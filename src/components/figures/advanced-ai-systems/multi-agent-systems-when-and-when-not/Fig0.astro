---
/**
 * advanced-ai-systems/0001 · Fig0 — the shared arrowhead <marker> defs.
 *
 * Copied VERBATIM from docs/advanced-ai-systems/lessons/0001-multi-agent-systems-
 * when-and-when-not.html (the off-screen `<svg width="0" height="0" …>` that
 * sits at the top of <body>). It is NOT a <figure> — it's the shared SVG the four
 * figures depend on: Fig1–Fig4 reference these markers by id (#arw, #arwG, #arwR,
 * #arwM) via marker-end/marker-start. Rendering it ONCE at the top of the lesson
 * body keeps the source's single-definition layout (no duplicate marker ids) and
 * makes the arrowheads resolve. Lives as an .astro component, NOT inline in the
 * MDX, because MDX's JSX parser is unreliable with raw inline SVG that uses
 * kebab-case attrs (view-box, marker-width, …) and self-referential ids.
 */
---

<svg width="0" height="0" style="position:absolute" aria-hidden="true"
    ><defs>
        <marker
            id="arw"
            viewBox="0 0 10 10"
            refX="8.5"
            refY="5"
            markerWidth="7"
            markerHeight="7"
            orient="auto-start-reverse"><path d="M0,0 L10,5 L0,10 z" fill="#6b675f"></path></marker
        >
        <marker
            id="arwG"
            viewBox="0 0 10 10"
            refX="8.5"
            refY="5"
            markerWidth="7"
            markerHeight="7"
            orient="auto-start-reverse"><path d="M0,0 L10,5 L0,10 z" fill="#2f5d3a"></path></marker
        >
        <marker
            id="arwR"
            viewBox="0 0 10 10"
            refX="8.5"
            refY="5"
            markerWidth="7"
            markerHeight="7"
            orient="auto-start-reverse"><path d="M0,0 L10,5 L0,10 z" fill="#9a2417"></path></marker
        >
        <marker
            id="arwM"
            viewBox="0 0 10 10"
            refX="8.5"
            refY="5"
            markerWidth="7"
            markerHeight="7"
            orient="auto-start-reverse"><path d="M0,0 L10,5 L0,10 z" fill="#b5179e"></path></marker
        >
    </defs></svg
>
