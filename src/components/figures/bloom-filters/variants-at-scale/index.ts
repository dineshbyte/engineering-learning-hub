// Barrel for the two bespoke bloom-filters/0004 SVG figures, so the MDX lesson
// can `import { Fig1, Fig2 } from '...'`. Each is a verbatim copy of the source
// SVG (see the per-file headers). Kept in a sub-folder co-located with the
// lesson's figures so the figures of one lesson never collide with another's.
export { default as Fig1 } from './Fig1.astro';
export { default as Fig2 } from './Fig2.astro';
