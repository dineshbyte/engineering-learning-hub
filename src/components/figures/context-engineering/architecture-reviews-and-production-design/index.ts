// Barrel for the three bespoke context-engineering 0005 SVG figures, so the MDX
// lesson can `import { Fig1, Fig2, Fig3 } from '...'`. Each is a verbatim copy of
// the source SVG (see the per-file headers). Kept in a per-lesson sub-folder
// co-located under the track, mirroring the bloom-0001 pattern.
export { default as Fig1 } from './Fig1.astro';
export { default as Fig2 } from './Fig2.astro';
export { default as Fig3 } from './Fig3.astro';
