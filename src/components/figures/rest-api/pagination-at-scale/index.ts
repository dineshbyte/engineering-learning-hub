// Barrel for the four bespoke rest-api/0006-pagination-at-scale SVG figures, so
// the MDX lesson can `import { Fig1, Fig2, Fig3, Fig4 } from '...'`. Each is a
// verbatim copy of the source SVG (see the per-file headers). Kept in a
// per-lesson sub-folder, co-located with the lesson's figures, so the bulk
// extraction never pollutes the shared components dir.
export { default as Fig1 } from './Fig1.astro';
export { default as Fig2 } from './Fig2.astro';
export { default as Fig3 } from './Fig3.astro';
export { default as Fig4 } from './Fig4.astro';
