# pnpm 10+/11 blocks dependency build scripts by default (supply-chain safety).
# Astro's toolchain needs these two: esbuild (bundler) and sharp (image optim).
allowBuilds:
    esbuild: true
    sharp: true
