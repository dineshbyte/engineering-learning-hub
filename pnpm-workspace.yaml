# epubjs pulls in core-js / es5-ext, which ship postinstall build scripts.
# We only vendor epub.js's prebuilt dist/, so those builds are unnecessary —
# disallow them to keep `pnpm install` clean and reproducible.
allowBuilds:
  core-js: false
  es5-ext: false
