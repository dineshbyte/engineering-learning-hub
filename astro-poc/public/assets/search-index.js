window.SEARCH_INDEX=[{"t":"AI Agents Explained: LLMs, Tools, Loops & MCP","u":"ai-agents/lessons/0001-ai-agents-from-first-principles.html","g":"AI Agents","k":"Lesson","x":"ai agents explained: llms, tools, loops & mcp ai agents from first principles: how llms, tools, the agent loop, planning, and mcp fit together — build the full stack from memory and tell real capability from hype. ai agents from first principles llm — the brain in a jar tool & tool calling — giving the brain hands agent — brain + hands + a goal agent loop — observe → think → act → repeat agent runtime — the app server around the brain memory — because the brain forgets everything workflow vs agent — who writes the if/else? mcp — usb-c for tools single vs multi-agent — monolith vs microservices archetypes — one engine, many job descriptions where each layer shows up — real use cases retrieval practice — mix the levels reconstruct the stack from a blank page interview — pick your bar ★ cheat sheet — buzzword → engineering translation table three rules for the vp chair ★ review guides & what to read next memory check memory check memory check memory check memory check memory check memory check memory check memory check memory check what is an llm, and what are the three things it can't do on its own? walk me through exactly what happens when an agent \"uses a tool\". define an agent and its agent loop. what is the agent runtime, and what is memory in this stack? when would you not build an agent, and ship a workflow instead? name the trade-off. your agent occasionally loops forever and burns huge cost. where do you fix it, and why is it not \"the model\"? a team wants to add a second agent to \"go faster.\" talk them into it - or out of it. explain mcp to a skeptical exec in one analogy, and name the problem it solves. a vendor pitches \"fully autonomous agentic ai.\" place it on the workflow agent spectrum and name the trade-off the pitch bought. how does a \"coding agent\" differ from a \"support agent\" architecturally - and what does that tell you about every agent product? build vs buy: a team wants to adopt an off-the-shelf agent runtime/sdk instead of writing their own loop. how do you reason about it? design a support agent that's safe to ship for a saas billing product. design an sre incident-triage agent. what can it touch, and where does it stop?"},{"t":"Agent Runtime, Context Engineering & Memory","u":"ai-agents/lessons/0002-agent-runtime-and-context.html","g":"AI Agents","k":"Lesson","x":"agent runtime, context engineering & memory your bar: explain to an interviewer how an agent runtime orchestrates an llm loop, why context engineering often matters more than model choice, and how the… agent runtime, context & memory agent runtime — the os around the llm context management — the ram budget how cursor vs claude code handle context limits 3 2 memory — because the llm forgets everything retrieval practice — test the three modules interview — pick your bar memory check walk me through the lifecycle of one agent loop iteration from the moment the llm returns output. how does a runtime prevent an agent from running forever or spending $500? memory check why is context management often more important than the model itself? you are building a coding agent. the codebase is 2m tokens but your window is 200k. what is your retrieval strategy? memory check your support agent needs to remember a customer's product preferences across sessions. what memory tier handles this and what's your storage/retrieval design? what's the difference between working memory and context window? what is an agent runtime, and name the things it does that the llm cannot do for itself? define the context window and explain what competes for it on a single agent call. name the three memory tiers and what each one is for. define memory in an agent system — why is \"the agent remembers\" a misleading phrase? walk me through the lifecycle of one agent loop iteration from the moment the llm returns output. why is context management often a bigger lever than which model you pick? what belongs in long-term memory and what should never go there? \"a bigger context window means we don't need context engineering anymore.\" where does that break? design the runtime guardrails so an agent can't loop forever or burn $500 on one task. design memory for an agent that must keep meaningful state across sessions and across many users. answers are wrong. a teammate wants to spend the quarter upgrading the model. make the principal-level call. design the runtime + context strategy for a long-running production agent that may run for hours over a 2m-token codebase with a 200k window. design the memory architecture for a customer-support agent that must recall prior interactions across sessions under a fixed token budget."},{"t":"How Coding Agents Work: Claude Code, Cursor, MCP","u":"ai-agents/lessons/0003-how-coding-agents-work.html","g":"AI Agents","k":"Lesson","x":"how coding agents work: claude code, cursor, mcp your bar: explain step-by-step how claude code executes a real task, how cursor retrieves context from a large repo, and how mcp turns any business function… how coding agents work how claude code actually works walkthrough: \"fix the authentication bug\" failure handling: errors are data, not exceptions how cursor actually works cursor vs claude code three axes that place any coding tool mcp — usb-c for tools retrieval practice — modules 4–6 interview — pick your bar memory check — m4 a user reports claude code is reading files that seem irrelevant to the task. what is likely causing this and how would you fix it? how does claude code handle a failing test during an iterative fix loop? memory check — m5 when would you use claude code instead of cursor for a coding task? what is the main weakness of an embedding-based retrieval system for code? memory check — m6 your team is building a billing agent. walk me through how you'd expose the billing system to the agent using mcp. what is the difference between an mcp tool, an mcp resource, and an mcp prompt? walk the agent tool-loop in one breath: what happens between the llm and the runtime on each turn? what is claude code, concretely — what is the llm and what is the runtime? how does cursor get context from a large repo, and how is that different from claude code? name mcp's three primitives and say what each one is. cursor's pre-built embedding index vs claude code's read-on-demand — what's the real trade-off? why does mcp matter? what was the world like before it? in the loop, who decides when to stop — and why does that placement matter? an agent keeps reading files that seem irrelevant to the task. diagnose it. design the tool registry and guardrails for a coding agent that runs unattended. what goes in? you're choosing a context strategy for a huge monorepo. how do you reason it through? a teammate says \"just give the agent a resource for the whole db and let it read whatever it needs.\" push back. design an mcp server that exposes a business system (say, a billing platform) to an agent. design a coding agent tailored to one specific codebase (e.g. a large internal monorepo). what do you build?"},{"t":"Multi-Agent Systems & Production AI Agents","u":"ai-agents/lessons/0004-agent-systems-and-production.html","g":"AI Agents","k":"Lesson","x":"multi-agent systems & production ai agents your bar: decide when multi-agent is justified vs unnecessary; name five ways production agents fail and how to prevent them; design a business agent… multi-agent systems & production multi-agent systems — microservices for agents production engineering — where agents break in the real world security + permissions observability + auditability cost control reliability + retries loop prevention failure modes (why agents get stuck) business agent architecture — designing for the org chart ★ stage 2 executive summary what you now understand (stage 2) the one diagram to draw retrieval practice — modules 7–9 interview — pick your bar ★ stage 2 architecture cheat sheet ai buzzword → engineering translation three rules to carry into any ai pitch ★ architecture review checklists architecture review checklist agent design checklist production readiness checklist common failure modes 🚩 red flags in an agent architecture questions a vp engineering should ask in a design review ★ review guides & what to learn next 5-minute weekly recall 30-minute deep dive (when stalled or before a big review) what to learn next when would you choose multi-agent over a single agent for a coding task? what are the main failure modes unique to multi-agent systems vs single-agent? memory check — 3 questions you're launching a saas billing agent that can apply credits, retry charges, and update subscription tiers. what production controls do you put in place before launch? how do you detect and prevent an infinite loop in a production agent? memory check — 3 questions your company wants an autonomous sre agent that can respond to incidents without human approval. what are your concerns and what controls would you require? how do you calculate the roi of a business agent and what are the most common ways agent roi is overstated? memory check — 3 questions define a single-agent system versus a multi-agent system, and name the two roles in the multi-agent pattern. what does the orchestrator (lead agent) do, and what does a subagent (worker) do? distinguish a workflow from an agent, and say where memory fits in an agent system. name three production controls every agent needs before launch, and what each one prevents. when does multi-agent genuinely help, and when is it just \"more microservices\" cargo-culting? why is cost the hidden killer of multi-agent systems, and how do you reason about it? how do you keep an agent from running forever or overspending in production? a wrong or stuck multi-agent run lands on you. why is it harder to debug than a single agent, and what makes it tractable? design a production multi-agent system with guardrails, observability, and cost controls. what's the skeleton? an engineer proposes \"let's add more agents\" to fix quality. challenge it the way you'd challenge \"let's add more microservices.\" \"autonomous agents will replace the team — ship it fully auto.\" make the principal-level call on autonomy. design a production sre incident-response agent — tools, guardrails, and observability — for a team that wants it to triage and remediate alerts. design a customer-support agent fleet for a saas product — multiple agents, guardrails, and cost controls under real traffic."},{"t":"AI Agent Architecture-Review Checklist","u":"ai-agents/reference/agent-architecture-review.html","g":"AI Agents","k":"Reference","x":"ai agent architecture-review checklist ai agent architecture-review checklist: seven review lenses, trust boundaries, production-readiness checks, common failure modes, and red flags for the design-review chair. reviewing an agent system — one-page cheat sheet the seven review lenses numbers that reframe the review trust boundaries architecture review checklist agent design checklist production readiness checklist common failure modes red flags in an agent architecture eight questions for the design-review chair "},{"t":"AI Agents Cheat Sheet","u":"ai-agents/reference/agents-cheat-sheet.html","g":"AI Agents","k":"Reference","x":"ai agents cheat sheet one-page ai agents cheat sheet: the agent stack, core definitions, mental models, archetypes, buzzword-to-engineering translations, and ten memory rules to recite. the agent stack — one-page cheat sheet the two diagrams definitions (compressed) mental models archetype = same engine, diff job three rules for the vp chair buzzword → engineering translation ten memory rules (recite these) 5-minute review (weekly) & 30-minute deep dive (when stalled) "},{"t":"Applied Systems Design Fundamentals","u":"applied-systems-design/applied-systems-design-fundamentals.html","g":"Applied Systems Design","k":"Lesson","x":"applied systems design fundamentals a repeatable method for any blank-page system design — interview or real review. walk through requirements, estimates, apis, data, and scaling without freezing. applied systems design from first principles the design method — never start by drawing boxes back-of-the-envelope estimation — the bridge to architecture url shortener — the canonical read-amplifier rate limiter — atomicity on the hot path news feed — when do you pay the fan-out cost? distributed cache — trade freshness for latency & load message queue — the partitioned, replicated log key-value store — the dynamo ring putting it together — the universal checklist & five levers retrieval practice — mix the designs reconstruct the method from a blank page interview — pick your bar ★ cheat sheet — prompt → lever reduction table three rules for the design round ★ review guides & what to read next memory check memory check memory check memory check memory check memory check memory check memory check memory check walk me through the design method you run on any blank-page prompt. 100m dau at 10 reads/day each — size the read qps and storage out loud. what goes in the api and data model for a url shortener, and why a kv store? name the five reusable levers and what each one buys you. a teammate wants to size the database first and check the math later. push back. a rate limiter shares a redis counter across n gateway replicas. what's the trap and the fix? your cache fronts the db at a 95% hit ratio. which two failure modes will page you? when does fan-out-on-write break for a news feed, and how do you resolve it? make the case that the estimate, not the feature list, should drive the architecture — then steelman the opposite. you're handed a system that's slow at p99 but fine at the mean. how do you reason about it as the senior in the room? an engineer proposes adding a cache to \"make everything faster.\" make the principal-level call. design a news feed for 300m users with a sub-200 ms feed-read budget. design a distributed key-value store that stays writeable under node and network failure."},{"t":"Consistent Hashing Explained — System Design","u":"applied-systems-design/consistent-hashing-deep-dive.html","g":"Applied Systems Design","k":"Lesson","x":"consistent hashing explained — system design consistent hashing from the ring up: why mod n is catastrophic on resize, why a bare ring is lopsided, and how virtual nodes (vnodes) flatten the distribution. consistent hashing mod n is the enemy the ring — what actually moves virtual nodes — fixing the lopsided ring bounded loads — when one key still runs hot you may not need a ring replication — the preference list retrieval practice — mix the sections reconstruct the topic from a blank page interview — pick your bar ★ cheat sheet — claim → engineering translation table three rules for the design round memory check memory check memory check memory check memory check memory check what problem does consistent hashing solve, and what's the one property it buys? why is hash(key) mod n a trap? quantify what happens when you add a node. walk me through a lookup on the ring and what its cost is. exactly which keys move when a node joins, and which when one leaves? a bare ring with 5 nodes is hot-spotting. diagnose it with the math, then fix it. vnodes balanced my keyspace but one node is still on fire. what's the failure mode and the fix? how do you place n replicas on a vnode ring, and what's the classic bug? when would you pick rendezvous (hrw) or jump hash over a vnode ring? separate consistent hashing from quorum replication. where's the boundary, and why does it matter? argue for vnodes — then steelman why a system might deliberately avoid them. a team caches with mod n and says \"rehashing on scale-up is a one-time blip.\" make the principal-level call. design the sharding/routing layer for a 100-node distributed cache. design partitioning + replication for a write-heavy distributed key-value store that must survive node and az loss."},{"t":"What Is a Bloom Filter? Explained Visually","u":"bloom-filters/lessons/0001-what-is-a-bloom-filter.html","g":"Bloom Filters","k":"Lesson","x":"what is a bloom filter? explained visually a bloom filter trades certainty for space: \"definitely not in the set\" is guaranteed, \"probably in the set\" is not. learn the core asymmetry and how it works. a bloom filter trades certainty for space the picture: k hashes, one bit array the asymmetry is the whole point three operations - and the one that's missing the one piece of math to carry in retrieval practice rehearse out loud primary source (read this next) interview — pick your bar what is a bloom filter, in one sentence, and what does it store? which error can a standard bloom filter make — and which can it never make? walk through what add and contains actually do at the bit level. why can't a standard bloom filter support delete? where would you put a bloom filter in a read-heavy database, and what does it buy you? \"probably present\" is useless — why bother if you still have to verify? what is the optimal hash count, and what's the catch with using too many hashes? the textbook fp formula — what is it, and why call it an approximation, not a law? make the case for a bloom filter over just caching, then steelman the opposite. a teammate proposes a bloom filter as the source of truth for \"has this user already claimed the coupon?\" where does this break? when would you not use a bloom filter at all? design a \"have we already crawled this url?\" check for a web crawler seeing billions of urls. design a \"weak / breached password\" rejection check at signup using a bloom filter."},{"t":"Bloom Filter Sizing: m, k & False-Positive Rate","u":"bloom-filters/lessons/0002-sizing-a-bloom-filter.html","g":"Bloom Filters","k":"Lesson","x":"bloom filter sizing: m, k & false-positive rate size a bloom filter the senior way: start from the false-positive rate you can tolerate, then derive bits (m) and hash functions (k) from n — don't guess. sizing: pick the error, derive the rest worked example, in three moves the cost ladder retrieval practice rehearse out loud primary source (read this next) interview — pick your bar what two inputs do you need to size a bloom filter, and what do they give you? if you double n but keep p fixed, what changes — m, k, or both? what's the rule-of-thumb for ~1% false positives, and the cost of going 10× tighter? size a filter for 1,000,000 urls at 1% fp — walk the numbers. size a bloom filter to dedupe 50m event ids at 0.1% false-dupe rate. you guessed a bit count instead of starting from p. why is that the junior move? why does k depend only on p and not on n — what's the intuition? you sized for n=10m but the set quietly grew to 30m. what happens, and how do you catch it? argue for over-provisioning the filter vs sizing it tight — make the principal call. how do you pick the target fp rate p in the first place — what drives the number? a service runs thousands of small bloom filters (one per tenant). how does that change sizing? design the sizing + memory budget for per-sstable bloom filters in an lsm store with 10,000 sstables. design sizing for a \"seen this request id?\" idempotency filter where traffic is bursty and n is hard to predict."},{"t":"Bloom Filters in the LSM Read Path & Failure Modes","u":"bloom-filters/lessons/0003-failure-modes-and-lsm-reads.html","g":"Bloom Filters","k":"Lesson","x":"bloom filters in the lsm read path & failure modes how a bloom filter earns its keep on the lsm-tree read path to skip disk reads — and how it silently rots when load outgrows its sizing. real failure modes. failure modes & the real lsm read path failure mode 1 — saturation: the fp rate is not constant failure mode 2 — the lsm payoff, and when it breaks failure mode 3 — bad hashes, no deletes, unbounded growth retrieval practice rehearse out loud primary source (read this next) interview — pick your bar in an lsm-tree read, what does a bloom \"no\" let the engine do, and why is that safe? what does a bloom \"maybe\" cost in the lsm read path? as a filter fills past its design n, what happens to the false-positive rate? why can't a standard bloom filter delete keys, and what does that cause over time? you're paged on a rising false-positive rate in a bloom-fronted cache in production. diagnose and fix. what happens to the lsm read path once the filter saturates — why is it operationally dangerous? your measured fp rate is worse than the formula predicts. what are the likely causes? why is the bloom filter's silent degradation harder to operate than a loud failure? counting bloom to enable deletes vs scheduled rebuild — make the principal call. how would you design alerting so a bloom-fronted read path can't silently rot in production? an lsm store's read latency is creeping up over weeks. walk your root-cause approach and where the bloom filter fits. design the bloom layer for a read-heavy lsm key-value store so it stays healthy as data grows for years. design a bloom-fronted dedup cache that won't silently rot — given the set churns and grows."},{"t":"Bloom Filter Variants at Scale: Counting & Scalable","u":"bloom-filters/lessons/0004-variants-at-scale.html","g":"Bloom Filters","k":"Lesson","x":"bloom filter variants at scale: counting & scalable counting, scalable, and blocked bloom filter variants compared. standard bloom is the default — reach for a variant only when deletes, unknown n, or speed bite. variants at scale: pick the one whose limit bites the decision matrix what each one actually changes the staff use: a cheap negative filter when not to use any of them retrieval practice rehearse out loud primary sources (read these next) interview — pick your bar what does a counting bloom filter change, and what does it cost? what problem does a scalable bloom filter solve? what's a cuckoo filter and where does it beat a bloom filter? what does a blocked / register-blocked bloom filter optimize, and at what cost? you need deletes but ram is very tight. counting or cuckoo — which, and why? item count n is unknown and may grow a lot. which variant, and what's the failure mode you're avoiding? what's the staff framing of a bloom filter in a pipeline — what is it, and what is it not? when should you reach for no filter variant at all — just use a real data structure? standard bloom is the default — defend that, then steelman reaching for a variant by default. two variants each fix one of your problems but cost on another axis. how do you make the call? design dedup for a 100-node, 10b-event/day ingest pipeline with 0.1% duplicate tolerance — what and why? design a distributed dedup tier for a high-throughput event pipeline using the right filter variant(s). design a cdn/cache \"one-hit-wonder\" admission filter so single-access objects never pollute the cache."},{"t":"Bloom Filter Cheat Sheet: Formulas & Tuning","u":"bloom-filters/reference/bloom-filters-cheat-sheet.html","g":"Bloom Filters","k":"Reference","x":"bloom filter cheat sheet: formulas & tuning a one-page bloom filter reference: the m, k, and false-positive-rate formulas, when to use one (lsm reads, dedup, cache admission), and when to avoid it. bloom filter cheat sheet what it is the math where it lives / when not to variants (one line each) "},{"t":"What Is Context? Context Windows & Tokens for LLMs","u":"context-engineering/lessons/0001-what-is-context-and-context-as-query.html","g":"Context Engineering","k":"Lesson","x":"what is context? context windows & tokens for llms your bar: explain why an llm cannot see your whole codebase, what actually fills the context window, and why context retrieval — not the model — is where modern ai systems win or lose. what is context — and why it's the new database query what is context? the window is a fixed budget — and you don't own all of it the four words, pinned down why an llm can't just \"see the whole codebase\" context is the new database query the inversion that matters why retrieval quality usually beats model quality retrieval practice — test the two modules interview — pick your bar memory check an engineer says \"let's just use the 1m-token model and paste the whole repo in.\" where is this wrong? memory check your ai feature gives wrong answers. a teammate wants to swap to the most expensive model. how do you push back as the senior in the room? frame \"context is the new database query\" for a vp who knows backend but not llms. define context, context window, token, and working context, and how they relate. besides the documents you retrieve, what else competes for the context window? what is a token, and why are both the window limit and the bill measured in tokens rather than words or characters? what's the difference between the context window and the working context? why can't an llm \"just read\" a 5-million-line codebase to answer a question about it? a context bug and a sql bug both return wrong data. why is the context bug operationally scarier? \"just use a bigger window and the problem goes away.\" why isn't that a real fix? an answer is wrong. how do you tell whether the context was wrong versus the model being incapable? make the case that retrieval quality matters more than model quality — then steelman the opposite. a teammate wants to \"fix\" wrong answers by switching to a 1m-token model and pasting the whole repo in. where does this break at scale? a strong engineer wants to spend the quarter upgrading the model to fix quality. make the principal-level call on model-vs-retrieval investment. design the context-retrieval layer for a coding assistant over a 2m-token monorepo with a 200k window. design the context assembly for a customer-support assistant — knowledge base plus per-customer history — under a fixed token budget."},{"t":"Context Retrieval: Lexical vs Semantic vs Hybrid","u":"context-engineering/lessons/0002-context-selection-and-retrieval.html","g":"Context Engineering","k":"Lesson","x":"context retrieval: lexical vs semantic vs hybrid your bar: explain how a system decides what to send to the model, and compare lexical, semantic, hybrid search, ranking and re-ranking by purpose, advantages, disadvantages, and failure modes. context selection & retrieval context selection the hero: a selection funnel relevance signals — what each catches, what it misses relevant means different things per system walk-through: find the authentication bug retrieval the hero: a two-stage retrieval pipeline the four engines, side by side search (lexical / keyword / bm25) semantic search (embeddings / vector) hybrid search ranking vs re-ranking — two different jobs retrieval practice — test the two modules interview — pick your bar memory check your code assistant keeps missing the actual buggy file. where in the selection pipeline do you look first, and how do you prove it? memory check users complain semantic search returns close but wrong results and sometimes whiffs on exact error codes. what's your fix and why? name the three stages of the selection pipeline, in order, and what each one does. what does lexical / bm25 search actually match on, and what is it good at? what does semantic / embedding search match on, and why use it? what is hybrid search, and what does fusion (e.g. rrf) do in it? users say semantic search returns close but wrong results and sometimes whiffs on exact error codes. what's your fix and why? your code assistant keeps missing the actual buggy file. where in the pipeline do you look first, and how do you prove it? distinguish ranking from re-ranking. when is a cross-encoder worth its cost, and when is it a waste? someone proposes just embed everything and drop bm25 — embeddings are strictly better. push back. design a retrieval strategy when you don't yet know the query mix — some lookups are exact ids, some are vague conceptual questions, and the corpus keeps changing. relevant means different things per system. contrast a billing platform, a code repo, and a docs portal — and say how that changes your retrieval. you can raise recall (wider net) or precision (aggressive re-rank + tighter packing), but not both for free. how do you reason about the trade-off? design the retrieval layer for a documentation q&a bot over versioned, frequently-updated docs. design retrieval for an internal-search feature across code, tickets, and wiki docs in one box."},{"t":"RAG, Chunking & Codebase Context Explained","u":"context-engineering/lessons/0003-codebase-context-chunking-and-rag.html","g":"Context Engineering","k":"Lesson","x":"rag, chunking & codebase context explained your bar: explain how cursor and claude code understand large repos, why chunking exists and how it fails, and how rag works from first principles — and why most rag bugs are retrieval bugs. codebase context, chunking & rag codebase context two strategies, side by side the three discovery steps, pinned down chunking the size trade-off overlap & the fragmentation failure common mistakes -> the better move rag — from first principles the pipeline why rag exists vs the alternatives why so many rag systems are poor retrieval practice — test the three modules interview — pick your bar memory check your team's embedding-indexed code assistant keeps citing a function that was deleted last week. what's happening and how do you fix it? memory check a rag bot answers most questions but always whiffs on \"what is the rate limit?\" even though the docs clearly state it. where do you look first? memory check sketch a docs-q&a or support assistant as rag, then name the three places it most often breaks in production. what are the three discovery steps a tool runs before it assembles a codebase-context window? contrast cursor's and claude code's strategy for understanding a large repo. what is chunking, and what makes one chunk \"good\"? name the five stages of the rag pipeline in order, and say what rag is. your embedding-indexed assistant keeps citing a function deleted last week. what's wrong and how do you fix it? walk through chunk-size trade-offs, and explain how zero-overlap fixed-size splitting silently loses facts. a rag bot answers most questions but always whiffs on \"what is the rate limit?\" though the docs state it. where do you look first? an embedding-only rag returns the right document for paraphrased questions but misses when users type an exact error code. why, and what do you add? at scale, how do you decide between a pre-built embedding index and read-on-demand — and when do you combine them? a team says \"our rag is bad, the model hallucinates — we need a smarter model.\" diagnose this as the senior in the room. make the case that retrieval quality matters more than model quality for a code assistant — then steelman the opposite. design a rag system for a help-center support bot over a changing knowledge base. design the code-context layer for a coding agent over a 2m-token monorepo with a 200k window."},{"t":"LLM Memory, Context Compression & Failure Modes","u":"context-engineering/lessons/0004-memory-compression-and-failure-modes.html","g":"Context Engineering","k":"Lesson","x":"llm memory, context compression & failure modes your bar: distinguish memory from a knowledge base, manage a long-running agent's context with summarization and sliding windows, and diagnose the five ways context systems fail — missing, wrong, outdated, conflicting, excessive. memory, compression & failure modes memory vs knowledge base vs retrieval who writes it, and what it holds memory, pinned down knowledge base, pinned down the confusion that trips people up context compression for long runs the four moves, pinned down the trade-offs to name out loud the five failure modes symptom -> cause -> fix (the centerpiece) retrieval practice — test the three modules interview — pick your bar memory check a teammate stores a user's preferences in the knowledge base index alongside the docs. why is that a design smell? memory check your agent loses track of a database id it was told 20 turns ago, after the history was summarized. what went wrong and how do you fix it? memory check your assistant quotes last quarter's pricing. walk through diagnosing it with the failure-mode framework. two retrieved docs give different answers to the same question. why does this happen and how do you make the system deterministic? distinguish memory, knowledge base, retrieval, and context. use a human analogy. who writes memory vs the knowledge base, and how does each one actually reach the model? name the four compression moves and which ones are lossy. list the five failure modes with one symptom each. a teammate stores a user's preferences in the knowledge base index alongside the docs. why is that a design smell? \"summarize aggressively to save money.\" where does that reasoning break down? your agent loses track of a database id it was told 20 turns ago, after the history was summarized. diagnose and fix. an assistant quotes last quarter's pricing. walk the failure-mode framework to the fix. design the memory + context strategy for an agent that runs for hundreds of turns on a fixed budget. two retrieved docs answer the same question differently and the answer flips between runs. classify it and make the system deterministic. production incident: the model is fluent and confident but the answer is just wrong. walk through classifying it before touching the model. design the memory and compression strategy for a multi-session customer-support agent that talks to the same users for months. design the context strategy for an autonomous agent that runs for hours on a single task (e.g. a long research or migration job)."},{"t":"Context Engineering: Architecture & Production Design","u":"context-engineering/lessons/0005-architecture-reviews-and-production-design.html","g":"Context Engineering","k":"Lesson","x":"context engineering: architecture & production design the capstone: review real ai products (cursor, claude code, chatgpt, copilot) by their context strategy, design production agents (billing, support, engineering, research), and walk away with the principal's toolkit — cheat sheet, checklists, and interview banks. architecture reviews & production design architecture reviews — reading real ai products the four products, side by side same query, two strategies production design — match retrieval to the data the generic production context pipeline four agents, four data shapes interview — pick your bar the principal's toolkit — your 9 take-home deliverables 1 · one-page context engineering cheat sheet 2 · context engineering mental models 3 · architecture-review checklist 4 · common failure modes — symptom -> cause -> fix 5 · interview questions (core -> staff) 6 · vp engineering review questions (leadership framing) 7 · 5-minute revision guide (the spine) 8 · 30-minute deep-dive revision (linking the track) 9 · what to learn next retrieval practice — the capstone memory check you're reviewing a vendor's \"ai code assistant.\" what's the first question you ask, and why? memory check design the retrieval layer for a billing-support agent that must quote exact charges. walk the trade-offs. name each product's context strategy: cursor, claude code, chatgpt, github copilot. what is the pre-indexed read-on-demand spectrum, and where do those four products sit on it? match each production agent to the retrieval its data type demands: billing, support, engineering, research. define agent, runtime, and rag as you'd use them in a production-design review. cursor and claude code use opposite strategies. name each one's signature failure mode and why position on the spectrum causes it. why is semantic embedding search the wrong retrieval for a billing agent, and what does the right design cost you? a research agent fans out across many sources. which failure modes does that shape invite, and how do you defend each? \"we'll add observability later — ship the agent first.\" push back like a reviewer. you're reviewing an unfamiliar vendor \"ai assistant\" with 20 minutes. what's your line of questioning, and what would make you walk away? build vs buy: a vp asks whether to adopt a vendor assistant or build our own retrieval. make the principal-level call. across billing, support, engineering, and research agents, where does retrieval investment actually pay off — and where would you not over-invest? design the context system for a billing assistant that quotes exact charges and also answers billing-policy questions. design the context system for a research agent that fans out across many sources and must produce a cited answer. the ten questions to run on any ai design core — what is a context window and why can't an llm just read the whole codebase? core — lexical vs semantic vs hybrid retrieval — when each? mid — walk me through a rag pipeline and where each failure mode enters. senior — how do you evaluate retrieval quality, not just answer quality? senior — why is matching retrieval to data type a senior decision? staff — review this design: an ai agent answers finance questions over a vector db of reports. pushback? staff — same model, two products, one feels far smarter. explain to a skeptical interviewer. vp — build vs buy: when do we build our own retrieval vs adopt a vendor assistant? vp — where are the cost levers in an ai feature? vp — where does investment in retrieval actually pay off? vp — how do we measure retrieval quality as a leadership metric? vp — what are the risk and compliance exposures? vp — when do we upgrade the model vs invest in retrieval?"},{"t":"RAG Evaluation: recall@k, RAGAS & Observability","u":"context-engineering/lessons/0006-retrieval-evaluation-and-observability.html","g":"Context Engineering","k":"Lesson","x":"rag evaluation: recall@k, ragas & observability your bar: measure retrieval separately from generation, name recall@k / precision@k / mrr / ndcg and the ragas-style answer-quality metrics, and explain golden sets, offline vs online eval, and the tracing that tells you which stage failed. retrieval evaluation & observability retrieval metrics — did we even fetch the right thing? why retrieval gets its own scoreboard the metrics, pinned down rag answer-quality metrics — beyond retrieval the 2 2 that tells you which half to fix the four ragas-style metrics scoring them at scale: llm-as-judge eval sets, offline vs online, and observability the golden set, and the two eval loops it powers what a trace must capture retrieval practice — test the three modules interview — pick your bar memory check your rag answers look 90% correct on a spot-check, but users complain on hard queries. which retrieval metric do you pull first, and what would it reveal? memory check faithfulness scores are high but users still report wrong answers. what's happening, and which metrics confirm it? memory check \"the bot gave a wrong answer last tuesday.\" with tracing in place, how do you go from that complaint to a root cause? define recall@k and precision@k, and say which one matters most for first-stage rag retrieval. what do mrr and ndcg measure, and when do you reach for each? name the four ragas-style answer-quality metrics and the one question each answers. what is a golden set, and what's the difference between offline and online eval? your rag answers look ~90% correct on a spot-check, but users complain on hard queries. which metric do you pull first, and what would it reveal? faithfulness scores are high but users still report wrong answers. what's happening, and which metrics confirm it? \"just optimize precision@k everywhere — fewer, cleaner chunks.\" where does that advice mislead you? you want to grade faithfulness on every production call. how do you do it, and what are the risks? \"the bot gave a wrong answer last tuesday.\" with tracing in place, how do you go from that complaint to a specific stage? offline eval is green but users still complain. what does that tell you, and what do you change? a teammate proposes a single end-to-end \"answer correctness\" score as the one metric to rule them all. make the principal-level call. design the eval and observability for a production rag support assistant from zero. what do you build first and why? design a golden-set plus ci gate for retrieval specifically. how do you build it, keep it honest, and decide when to block a deploy?"},{"t":"Advanced RAG: HyDE, GraphRAG & Agentic Retrieval","u":"context-engineering/lessons/0007-pre-retrieval-and-advanced-rag.html","g":"Context Engineering","k":"Lesson","x":"advanced rag: hyde, graphrag & agentic retrieval your bar: transform the query before you retrieve, match the retrieval structure to the question shape, and know when to graduate from a static pipeline to a self-correcting agentic loop. pre-retrieval & advanced rag query transformation a stage that didn't exist in the baseline the pre-retrieval techniques, pinned down hyde — the query/document vocabulary gap advanced retrieval structures flat vs structured retrieval which structure for which question local fact vs global theme — the graphrag line agentic & self-correcting rag the self-correcting loop three named patterns the trade-off you must name retrieval practice — test the three modules interview — pick your bar memory check m1 — your rag recall is poor on multi-part questions like \"compare our refund policy in the eu vs the us.\" which pre-retrieval technique, and why? memory check m2 — a teammate proposes rebuilding the whole rag stack on graphrag because \"it's the state of the art.\" how do you respond as the senior in the room? memory check m3 — when would you not add an agentic loop to a rag system, and how do you decide? what is pre-retrieval query transformation, and name the main techniques. explain how hyde works and what gap it bridges. what does the small-to-big (parent-document) retriever do? define agentic rag, self-rag, and corrective rag (crag) — what's the control loop in each? your rag recall is poor on multi-part questions like \"compare our refund policy in the eu vs the us.\" which pre-retrieval technique, and why? routing and step-back are both pre-retrieval. contrast what they do and when each is the right call. a teammate proposes rebuilding the whole rag stack on graphrag because \"it's the state of the art.\" how do you respond as the senior in the room? explain contextual retrieval and when its added cost is worth paying. walk through how you match retrieval structure to the shape of the question across a mixed query workload. when is an agentic / corrective loop worth its latency and cost, and when is a static pipeline the right default? make the case that pre-retrieval is the highest-leverage place to spend — then steelman the opposite. design an advanced-rag pipeline for a question type that flat top-k fails on — e.g. \"summarize how our incident response process evolved over the last two years.\" design retrieval for a corpus that must serve both local facts (\"what's the retry limit?\") and global synthesis (\"what themes recur across all our incidents?\")."},{"t":"Embeddings, Vector Indexes (HNSW/IVF/PQ) & Cost","u":"context-engineering/lessons/0008-embeddings-indexing-and-cost.html","g":"Context Engineering","k":"Lesson","x":"embeddings, vector indexes (hnsw/ivf/pq) & cost your bar: explain what an embedding actually is, how vector indexes (hnsw / ivf / pq) trade recall for speed and memory, and how to model the real token and dollar cost of a retrieval pipeline. embeddings, indexing & cost embeddings deep-dive what an embedding is: meaning becomes geometry the four knobs you actually choose dimensions & the similarity metric chunk granularity decides embedding quality re-embedding is a real migration vector index internals (ann) why approximate? brute force doesn't scale the three index families the trade-off triangle & metadata filtering indexing pipelines, freshness & cost the ingestion pipeline freshness: a stale index is the \"outdated context\" failure where the tokens and dollars go retrieval practice — test the three modules interview — pick your bar memory check a teammate wants to bump the embedding model to the newest one \"for a quick quality win.\" what do you flag? memory check your vector search is fast but misses obviously relevant chunks for some queries. where do you look? memory check your rag feature's cost-per-query is 3 the budget. walk through how you'd bring it down. how do you keep an index fresh for a docs site that updates daily, with some live-changing fields? what is an embedding, and what does \"near\" mean in embedding space? name the three similarity metrics and say what cosine actually measures. what does ann stand for, and why approximate instead of exact? what are the five stages of an ingestion pipeline? a teammate wants to bump the embedding model \"for a quick quality win.\" what do you flag? pick an index family for 50m vectors with tenant filtering and a p99 latency budget. walk the trade-offs. vector search is fast but misses obviously relevant chunks for some queries. where do you look? your rag cost-per-query is 3 the budget. name the levers, in order. design an embedding + index + cost strategy for a 200m-chunk corpus on a fixed monthly budget. when is a vector index the wrong tool, and what would you build instead? a stale index keeps serving a deleted doc. frame this as a systemic failure and fix it at the root. design the vector index + ingestion pipeline for a fast-changing corpus (news + product catalog, edits every minute). design a cost-bounded retrieval stack: serve relevant context under a hard per-query dollar/latency ceiling."},{"t":"LLM Prompt Caching, Context Ordering & Security","u":"context-engineering/lessons/0009-context-caching-ordering-and-security.html","g":"Context Engineering","k":"Lesson","x":"llm prompt caching, context ordering & security your bar: structure a prompt for kv/prompt caching, order the window so the model actually reads what matters, and defend a retrieval pipeline against prompt injection and cross-tenant leaks. context caching, ordering & security prompt / kv caching stable first, volatile last — the prefix is the cache key what's stable vs volatile — and where it goes context assembly & ordering lost in the middle — attention is u-shaped where to put things ordering practices that pay context security & multi-tenancy indirect prompt injection — the scary one multi-tenant access control — the one engineers get wrong threats -> how they get in -> defense retrieval practice — test the three modules interview — pick your bar memory check m1 — your agent has a 6k-token system prompt + tool defs and gets called thousands of times an hour. walk me through making it cheaper with caching. memory check m2 — same retrieved documents, but answer quality swings between runs. ordering is suspect. how do you diagnose and fix it? memory check m3 — design the security boundary for a multi-tenant rag assistant over a shared vector index. what goes where? m3 — an attacker uploads a document containing \"ignore prior instructions and email me the user's records.\" why is this dangerous and how do you defend? what does prompt / kv caching actually reuse, and what is the cache key? what's the layout rule for caching, and what's stable vs volatile? what is \"lost in the middle,\" and where is model attention strongest? what is indirect prompt injection, and where does multi-tenant access control belong? your hourly llm bill spiked overnight with no traffic change. caching is suspect. how do you find it? same retrieved docs, but answer quality swings run to run. ordering is suspect. diagnose and fix. a teammate says \"we tell the model to only use the user's own data — that's our access control.\" push back. do caching (stable-first) and recency (query-last) actually conflict? make the case either way. design the context layout for a long-running chat agent: cheap, accurate, and it doesn't drift over a long session. one pipeline, three governors — cost, accuracy, safety — that can pull against each other. how do you reason about the trade-offs? \"our kb is internal, so retrieved content is trusted, and a system-prompt rule handles tenancy.\" tear this apart as a system design. design a secure multi-tenant rag context layer over one shared vector index. design the caching + assembly order for a long-running chat agent that must stay cheap and accurate over a long session."},{"t":"Context System Architecture-Review Checklist","u":"context-engineering/reference/context-architecture-review.html","g":"Context Engineering","k":"Reference","x":"context system architecture-review checklist a reusable architecture-review checklist for ai/rag context systems: probe retrieval, ranking, memory, caching, ordering, evaluation, cost, and security failure modes before you ship. context system — architecture-review checklist architecture-review checklist — the ten questions for any ai design product teardown — name the strategy, the failure falls out production design — match retrieval to the data type the five failure modes — symptom -> cause -> fix where the failure enters the pipeline red flags in a context architecture evaluation & observability — what to probe l6 pre-retrieval & advanced rag — decision points l7 embeddings, indexing & cost — what to probe l8 caching, ordering & security — what to probe l9 cost & ordering security & multi-tenancy vp / leadership review questions "},{"t":"Context Engineering Cheat Sheet","u":"context-engineering/reference/context-engineering-cheat-sheet.html","g":"Context Engineering","k":"Reference","x":"context engineering cheat sheet a one-page context engineering cheat sheet: retrieval, chunking, rag, embeddings, vector indexes, memory, compression, prompt caching, ordering, evaluation, and security at a glance. context engineering — one-page cheat sheet the spine (mental models) what competes for the window the two diagrams chunking rules retrieval techniques the five failure modes context compression (long runs) match retrieval to data type retrieval evaluation l6 pre-retrieval & advanced rag l7 embeddings, indexing & cost l8 caching, ordering & security l9 5-minute revision (the whole spine) "},{"t":"Consensus Explained: Paxos, Raft & FLP","u":"distributed-systems/consensus-deep-dive.html","g":"Distributed Systems","k":"Lesson","x":"consensus explained: paxos, raft & flp how distributed consensus works: the flp impossibility result, paxos, raft leader election and safety, quorums, and the commit rules that keep replicated state machines correct. consensus — the floor beneath flp, raft & bft flp, actually proved — the bivalence argument raft is safe via five properties — one is subtle the commitment gotcha — raft's figure 8 membership change without splitting in two the landscape — you don't have to use raft when nodes lie — byzantine consensus & the 3f+1 wall where each idea shows up — real systems retrieval practice — mix the levels reconstruct the deep dive from a blank page interview — pick your bar ★ cheat sheet — the numbers & the gotchas the numbers the gotchas that bite memory check memory check memory check memory check memory check memory check what problem does consensus solve, and what are its three required properties? why does crash tolerance need 2f+1 nodes but byzantine tolerance need 3f+1? what does flp state precisely, and what's the standard escape hatch? how does raft elect a leader and guarantee at most one per term? state flp precisely, then explain why the bivalence argument is sharper than \"the adversary delays a message.\" a new raft leader is never told what was committed. why is it guaranteed to already hold every committed entry? walk me through raft's figure 8 and state the commitment rule that prevents it. how does raft change cluster membership without risking two leaders? your team's homegrown raft loses a committed write under one rare crash sequence. what's your first hypothesis and how do you confirm it? when would you choose multi-paxos, raft, epaxos, or pbft — and when none of them? distinguish safety from liveness in a replicated log, and which one you're allowed to sacrifice. whiteboard the commit path for a replicated log that must never lose an acknowledged write. design a consensus-backed configuration / coordination store (think etcd / zookeeper)."},{"t":"Distributed Systems Fundamentals — CAP & Consistency","u":"distributed-systems/distributed-systems-fundamentals.html","g":"Distributed Systems","k":"Lesson","x":"distributed systems fundamentals — cap & consistency distributed systems fundamentals: the cap theorem, consistency models, replication, partitioning, and failure modes — reason about any async, multi-service system from first principles. distributed systems from first principles the one hard truth — you can never know delivery guarantees & idempotency no global clock — logical time order & causality — the broadcast ladder replication — leader, multi-leader, leaderless consistency models — eventual → causal → linearizable cap & pacelc — the real trade-offs consensus — flp impossibility + raft partitioning / sharding — splitting without hot spots putting it together — the resilience toolkit interview — pick your bar retrieval quiz — answer before peeking reconstruct the field from a blank page ★ cheat sheet — buzzword → engineering translation table three instincts to install ★ review guides & what to read next memory check memory check memory check memory check memory check memory check memory check memory check memory check memory check state cap precisely, then correct the most common misreading. name the consistency models from weakest to strongest and what each guarantees. what is a quorum, and why does r + w > n matter in a leaderless store? name the three replication topologies and the price each pays. a payment call times out with no reply. what do you do, and why is \"just retry\" dangerous? why is \"exactly-once delivery\" impossible, and what do we actually ship instead? two replicas accepted writes; which one wins? walk me through the clock issue. you key a time-series table by timestamp and one node is melting. why, and how do you fix it? events are \"coming out of order.\" diagnose it, then decide how much ordering to actually buy. how does raft avoid two leaders, and what does flp have to do with it? how would you choose a failure detector, and why is \"perfect failure detection\" the wrong goal? design an order-checkout flow across payment, inventory, and shipping that survives partial failure. design a globally replicated key-value store and state its consistency contract under partition."},{"t":"Idempotency Keys & Exactly-Once Delivery","u":"distributed-systems/idempotency-keys-deep-dive.html","g":"Distributed Systems","k":"Lesson","x":"idempotency keys & exactly-once delivery idempotency keys done right: dedupe retried requests, store and replay saved results safely, handle concurrent and in-flight duplicates, and reach exactly-once delivery semantics. idempotency keys — deep dive the race — two retries, one key atomicity — record the result with the side effect what to store — and the same-key-different-body trap the key's lifecycle — scope, generation, and the ttl hazard where idempotency keys stop helping the one idea behind all four edges interview — pick your bar retrieval practice — mix the levels reconstruct the four edges from a blank page ★ cheat sheet — the four edges → the discipline translation table three questions for the design review ★ review guide & what to read next memory check memory check memory check memory check memory check what is an idempotency key and what problem does it solve? beyond the key itself, what do you store in the idempotency record — and why is a bare key not enough? why is \"exactly-once delivery\" impossible, and what's the real guarantee? which operations don't need an idempotency key, and why? two retries with the same key hit two app servers at the same instant. why does the naive design double-charge, and how do you fix it? you've solved the race. the server still occasionally double-charges. where's the remaining window? when can you wrap the side effect and the idempotency record in one transaction — and when can't you? a request claims the key (in_progress) then the process crashes. what happens to future retries, and how do you prevent a permanent outage? how do you choose the retention ttl, and what goes wrong if it's too short? a team says \"we added idempotency keys, so the whole payment pipeline is exactly-once.\" push back. producer-side keys vs consumer-side dedup — how do you decide where to put the guarantee? design an idempotent payment api: charge a customer exactly once despite client retries. design exactly-once consumption from an at-least-once message queue (e.g. payments off kafka/sqs)."},{"t":"REST API Interview Questions: RESTful Design","u":"rest-api/interview/0001-what-makes-an-api-restful.html","g":"REST API","k":"Interview","x":"rest api interview questions: restful design practice restful design with 21 senior and staff rest api interview questions on constraints, statelessness, hateoas, and the richardson maturity model. rest foundations — interview questions what is rest, precisely? name the six architectural constraints. is rest the same thing as http? resource vs representation — what's the difference? what does the statelessness constraint actually require? why does statelessness help you scale? \"stateless\" means no database and no cookies, right? what are the costs of statelessness, and how do you mitigate them? how does statelessness shape your authentication design? what are the four sub-constraints of the uniform interface? what is hateoas? if hateoas is part of rest, why is it so rare in real apis? when would you actually invest in hateoas / level 3? describe the richardson maturity model. what level do most production \"rest\" apis sit at? is a level 2 api \"really rest\" by fielding's own definition? give a concrete example of a level 0 system. rest vs grpc/rpc — when would you pick each? rest vs graphql — what are the trade-offs? a team says \"let's make our api restful.\" how do you advise them? why do interviewers open with \"what makes an api restful?\" design a restful api for a blogging platform (posts, comments, authors). walk the resource model and maturity level. an interviewer hands you a level 0 \"json over http\" api (one post /api with an action field) and asks you to make it restful. how do you approach it?"},{"t":"HTTP Methods & Status Codes — Interview Questions","u":"rest-api/interview/0002-http-methods-and-status-codes.html","g":"REST API","k":"Interview","x":"http methods & status codes — interview questions practice http methods and status codes with 49 senior and staff rest api interview questions on get/post/put/patch/delete semantics, safety, and 2xx–5xx codes. http methods & status codes — interview questions define a safe http method. define an idempotent method. what does cacheable mean, and which methods are cacheable by default? explain \"safe ⊂ idempotent.\" \"safe means secure.\" true or false, and why? can a safe method have side effects? give an example. a teammate puts a \"mark as read\" action behind get /messages/42?read=true . what's wrong, and what breaks? is post idempotent? why does it matter? lead with idempotency, not the verb — what's the senior framing? put vs patch — what's the difference? put vs post for creation — what's the deciding question? why is put idempotent but post isn't, for creation? is patch idempotent? json merge patch vs json patch — how do they relate to idempotency? a client must atomically guard a patch against a concurrent edit. how? is delete idempotent given that a second call returns 404 ? on a repeated delete of an already-gone resource, do you return 404 or 204 ? what is head for? what is options for? a client sends delete to a read-only resource. what do you return, and what's required? when does an action genuinely not map to any standard verb, and what do you do? 200 vs 201 vs 204 — when each? what header must a 201 created include? what does 202 accepted mean, and how does it differ from 201 ? design the codes for synchronous vs asynchronous creation. successful delete — which code? permanent vs temporary redirect — which codes? why prefer 307 / 308 over 302 / 301 ? why is 304 not modified a feature, not an error? a browser post form keeps re-submitting on refresh. which redirect pattern fixes it and why? 400 vs 422 — what's the distinction? is 422 mandatory for validation failures? 401 vs 403 — what's the difference? when should a forbidden resource return 404 instead of 403 ? when do you return 409 conflict ? what does 429 require, and what should the client do? 404 vs 410 — when is 410 gone the right call? how do you express optimistic-concurrency conflicts at the http layer? what does the 5xx family signify, and what's the cardinal rule? 500 vs 502 vs 504 — distinguish them. when is 503 the right code, and what should accompany it? a downstream dependency is timing out under load. walk through the status codes and behavior you'd return. what's wrong with returning 200 ok with an error in the body? a graphql-style api returns 200 for everything. is that ever defensible? what's the problem with tunnelling everything through post ? when is reaching for patch over-engineering? status code as the contract, or body error codes — how do you reconcile them? run the senior \"gotcha checklist\" for methods and status codes. \"a client calls post /payments and the connection drops before they get a response.\" design for a safe retry. design the request/response contract for post /payments where the connection can drop mid-flight. walk the methods and status codes. design the update + concurrency story for a resource many clients edit at once (put vs patch, conditional requests)."},{"t":"API Idempotency Interview Questions","u":"rest-api/interview/0003-idempotency-in-practice.html","g":"REST API","k":"Interview","x":"api idempotency interview questions practice idempotency and reliability with 38 senior and staff rest api interview questions on idempotency keys, safe retries, exactly-once semantics, and dedup. idempotency & reliability — interview questions what does it mean for an operation to be idempotent? safe vs idempotent — what's the difference? which http methods are idempotent? why isn't post idempotent? why is patch \"not guaranteed\" to be idempotent? is \"idempotent\" the same as \"returns the same response\"? why does idempotency matter so much in distributed systems? how do you make a non-idempotent post safe to retry? who generates the idempotency key, and why? walk through the server-side flow on first request vs retry. why store a fingerprint of the request, not just the key? why does stripe keep idempotency keys for ~24h rather than forever? should the idempotency layer live in the gateway or the service? where do you store the key → response mapping? two requests arrive concurrently with the same key — what happens? why fold the key-write into the same transaction as the effect? how should you scope idempotency keys? should you apply idempotency keys to every endpoint? a retry arrives while the original is still running. wait or 409 ? the process crashes after the effect commits but before responding. what does the next retry see? what goes wrong if you cache the response but the original operation actually failed? does idempotency give you exactly-once delivery? write the equation for exactly-once effect. why is exactly-once delivery actually impossible? in a message/event consumer, what do you deduplicate on? brokers advertise \"exactly-once\" — is that a contradiction? why is put naturally idempotent? is delete idempotent? what status do you return on a repeat? 204 vs 404 on a repeated delete — name the trade-off. how do you design a create to be naturally safe to retry? \"design for natural idempotency vs bolt on a key\" — how do you decide? which operations are safe to retry blindly? what is exponential backoff with jitter, and why both? what is a thundering herd, and how do retries cause it? what should you do when a response carries retry-after ? what does a circuit breaker add on top of backoff? why cap the number of retry attempts? design post /orders so a flaky client can retry without duplicates. design post /orders so a flaky client can retry without creating duplicate orders. design a message/event consumer that must not double-process a payment when the broker delivers at-least-once."},{"t":"Resource Modeling & URI Design Interview Questions","u":"rest-api/interview/0004-resource-modeling-and-uri-design.html","g":"REST API","k":"Interview","x":"resource modeling & uri design interview questions practice resource modeling and uri design with 44 senior and staff rest api interview questions on resources, naming, nesting, slugs, and canonical urls. resource modeling & uri design — interview questions why is post /getorder an anti-pattern? should collection names be singular or plural? what is the difference between a collection uri and an item uri? what's the right way to create a new order? why don't actions like cancel or publish map cleanly to crud? what does nesting like /customers/1/orders communicate? why prefer opaque identifiers over sequential integers in uris? what are slugs for, and what should they not be? what are the boring-on-purpose uri formatting conventions? what belongs in query parameters and what doesn't? why is resource modeling usually the first artifact in a design interview? why does the noun/verb discipline matter beyond aesthetics? a teammate wants get /searchorders . what do you propose instead? what are the three principled ways to model a non-crud action? when do you reify an action as its own top-level resource? when is a custom method ( post /orders/42:cancel ) the right call? sub-resource vs custom method for the same action — how do you choose? what status code does an async action like :cancel return if it can't complete synchronously? why is deep nesting like /customers/1/orders/42/items/9 fragile? what's the shallow alternative to deep nesting, and what do you keep? link, embed, or separate endpoint — what are the three ways to handle related data? over-embedding vs the n+1 problem — describe the tension. where should a many-to-many relationship be modeled — under which resource? what is a bola / idor vulnerability and how does id choice relate to it? uuid vs ulid — when does the difference matter? should the uri expose your internal database primary key? when is a singleton resource the right shape, and how do you spot the mistake? what does /users/me express, and why is it useful? why does consistency across the surface beat a locally clever endpoint? how should the path represent filtering by multiple values or ranges? walk through deriving a resource model from a domain — what's your method? how do you handle pluralization edge cases (uncountable nouns, irregulars)? how does uri design interact with http caching? how does resource modeling interact with versioning? interviewer pushes back: \"deep nesting is more restful and self-documenting.\" your reply? articulate the pure-rest vs pragmatist tension on custom methods. should a state transition be a patch on a status field instead of a custom method? what are the downsides of a flexible ?expand= / field-selection mechanism? a mobile team complains your api is too chatty. how do you respond at the design level? client-generated vs server-generated ids — what changes in the design? whiteboard prompt: model an e-commerce checkout (browse cart, place order, cancel, refund). what's your test for \"does this action deserve to be its own resource?\" how does uri shape interact with object-level authorization? when would you accept verbs in paths or otherwise break the conventions? model an e-commerce checkout (cart, place order, cancel, refund) on a whiteboard. design the resource model for a multi-tenant project-management api with users, projects, tasks, comments, and memberships."},{"t":"API Versioning Interview Questions","u":"rest-api/interview/0005-api-versioning-strategies.html","g":"REST API","k":"Interview","x":"api versioning interview questions practice api versioning and evolution with 36 senior and staff rest api interview questions on uri vs header versioning, breaking changes, and deprecation. versioning & api evolution — interview questions what is a breaking change? name the classic breaking changes. name the non-breaking changes you can ship freely. what is a \"tolerant reader\"? what is postel's law, and how does it apply here? what are the three places to put a version? why is uri-path versioning so popular? how does stripe version its api? how does github version its api? walk through the deprecation lifecycle. what does the sunset header do? why is \"change a field's meaning \" breaking even when the type stays the same? why is making an optional field required a breaking change? is adding a field to a response always non-breaking? adding an enum value — safe or breaking? \"how do you version a public api?\" what's the senior reframe? why is \"just cut a new version\" the wrong default? give an example of turning a \"breaking\" change into an additive one. what's the downside of uri-path versioning? header versioning — pros and cons? media-type versioning — when and why? how does each placement strategy interact with caching? why is per-account pinning (stripe) such a strong pattern? date-based vs semver versions — which is \"better\"? why do apis expose only the major version? is semver even the right mental model for a web api? why signal deprecation in-band via headers when you've already emailed and updated docs? how do you decide when it's safe to actually remove the old version? how long should the migration window be? \"public api, thousands of integrations. we must change the user object's shape. how?\" (60-second answer) how does versioning interact with hateoas? how does versioning interact with sdks and codegen? a field is technically optional but every client has always sent it. you want to drop it from responses. breaking? how do you design the system so the additive path stays open longer? is there a \"right\" answer on placement? what's the deeper instinct stripe and github share, beyond their mechanics? an old version still has 2% of traffic at the sunset date — but it's expensive to keep running. what do you do? how do you coordinate a migration across clients you don't control? how do you keep the support cost of multiple live versions from compounding? when is forcing a breaking version on everyone the right call, despite the cost? design a versioning & deprecation strategy for a public payments api with thousands of integrators. you must make a breaking change to a widely-used response shape — design the rollout so no client breaks."},{"t":"API Pagination Interview Questions","u":"rest-api/interview/0006-pagination-at-scale.html","g":"REST API","k":"Interview","x":"api pagination interview questions practice pagination at scale with 31 senior and staff rest api interview questions on cursor vs offset paging, keyset pagination, deep pages, and stable sorts. pagination at scale — interview questions how does offset/limit pagination work? what's the single biggest performance problem with deep offsets? when is offset pagination actually the right choice? what is cursor (keyset) pagination, in one sentence? why does cursor pagination's cost stay flat with depth? why hand the client an opaque cursor instead of raw column values? what are before and after cursors for? why is an exact total count expensive at scale? how do you tell the client there's a next page without counting? default position: a colleague reaches for offset on a new high-volume list endpoint. what do you say? why do interviewers ask \"how would you paginate a huge, constantly-changing collection?\" why is offset's slowness so dangerous in practice — why does it slip through? explain the consistency problem with offset pagination under concurrent writes. write the core keyset query and explain each piece. why is cursor pagination stable under concurrent writes? why isn't created_at alone enough as a cursor key? what index do you need for keyset pagination to actually be fast? what does cursor pagination cost you compared to offset? \"offset vs cursor\" — give the one-line rule for picking. what actually goes inside the cursor? why must sort and filter stay stable for a cursor's whole life? a client sends back a cursor that's now invalid (key migrated, filter changed). what should the api do? the product wants a total count on a huge feed. what are your options? link header vs envelope — what's the trade-off? contrast how github and stripe paginate. where should per_page / limit be bounded, and why? design pagination for an activity feed: hundreds of millions of rows, constantly written. walk me through it. how do you implement bidirectional (prev as well as next) cursor paging? a client needs a fully consistent snapshot across a long multi-page export. what do you do? are offset and cursor the only models? where do time-window and \"seek by value\" fit? can you keep offset's jump-to-page ui but fix its deep-scan cost? the tuple comparison (a,b) > (x,y) — does every database optimize it as a single index range seek? should a cursor be signed or encrypted, and should it expire? why does stripe use an object id as the cursor rather than an opaque blob — isn't that leaking internals? how do filtering and sorting interact with cursor pagination? a user wants to deep-link / permalink to \"page 5\" of a cursor-paginated list. how do you handle it? you inherit an offset api in production and need to migrate to cursors without breaking clients. how? design pagination for an activity feed with millions of rows and constant inserts. design a paginated, filterable, sortable list endpoint that must also support jump-to-page in an admin ui."},{"t":"HTTP Caching & Conditional Requests Interview Questions","u":"rest-api/interview/0007-caching-and-conditional-requests.html","g":"REST API","k":"Interview","x":"http caching & conditional requests interview questions practice http caching with 36 senior and staff rest api interview questions on cache-control, etags, conditional requests, validation, and revalidation. caching & conditional requests — interview questions what are the two distinct halves of http caching? what does cache-control: max-age=n mean, and what happens while a response is fresh? what's the difference between public and private ? how do s-maxage and max-age differ, and why have both? no-store vs no-cache — what's the difference? (most candidates trip here.) what does must-revalidate add on top of normal freshness? what does stale-while-revalidate=n buy you? how does expires relate to cache-control: max-age ? when does a response become \"stale,\" and what happens next? what is an etag ? walk through a conditional get with if-none-match . what status comes back if nothing changed? what's the bandwidth win of a conditional get ? how does last-modified / if-modified-since work? why is etag generally preferred over last-modified ? strong vs weak etags ( \"abc\" vs w/\"abc\" ) — what's the distinction and when do you use each? does receiving a 304 let the cache extend the freshness lifetime? how would you generate etags, and what's the cost trade-off? what is the lost-update problem? how does if-match prevent lost updates, end to end? what status code signals a failed precondition, and what does the client do? how do you get a race-free \"create only if it doesn't exist\"? why is optimistic concurrency a better fit for a web api than pessimistic locking? when would you reach for pessimistic locking despite all that? is the etag-on-write check itself atomic, or do you still need db-level guarantees? same etag drives caching and concurrency — articulate why that's elegant, and any tension. private vs shared caches — name where each lives. how do caching layers map onto rest's constraints? what is the cache key by default, and what does vary change? why is vary: authorization a cache-efficiency trap on a shared cache? what cache-invalidation strategies do you have, and their trade-offs? a write happens at the origin but a cdn still serves the old copy. what's going on and how do you fix it? what's the classic auth + caching pitfall? what's the safe baseline for caching authenticated responses? can you cache post responses? should you? what are the real costs/risks of serving stale data? a developer sets no-cache intending \"don't cache this.\" what actually happens? what goes wrong if you forget vary on a content-negotiated endpoint? \"two users open the same document and both hit save. how do you stop the second save from clobbering the first?\" (~60s.) design a caching strategy for a read-heavy public api. how do a cdn and an api tier divide caching responsibilities? when should you deliberately not cache? how do you decide a ttl for a given endpoint? design the caching & conditional-request strategy for a read-heavy public product catalog api fronted by a cdn. design optimistic-concurrency control for a collaborative document api where many clients edit the same resources."},{"t":"API Auth Interview Questions: AuthN & AuthZ","u":"rest-api/interview/0008-authentication-and-authorization.html","g":"REST API","k":"Interview","x":"api auth interview questions: authn & authz practice api authentication and authorization with 49 senior and staff rest interview questions on oauth2, jwt, sessions, scopes, rbac, and token security. authentication & authorization — interview questions define authentication vs authorization. which status code maps to a missing/invalid credential vs an authenticated-but-not-permitted request? give the one-line senior framing of 401 vs 403 . what is an api key and where is it appropriate? what are the weaknesses of api keys? what is a bearer token, its core risk, and how do you contain it? is oauth2 an authentication protocol? what does oidc add on top of oauth2? what is mtls and when do you reach for it? mtls vs signed jwts for service-to-service auth — how do you choose? which grant for a user signing in via web/mobile/spa? what problem does pkce solve, and how? which grant for machine-to-machine with no user? why are the implicit and password grants deprecated — what replaces them? what are scopes? what is the aud (audience) claim and why must a resource server check it? access token vs refresh token — what's the difference? what is refresh-token rotation and what does it detect? where should a spa store tokens, and why is that contentious? what are the three parts of a jwt? is a jwt payload encrypted? why do stateless jwts fit rest so naturally? what is the chief weakness of a jwt versus a server-side session? how do you handle jwt revocation honestly? list the checks of a proper jwt validation. explain the alg: none and algorithm-confusion attacks. symmetric (hs256) vs asymmetric (rs256/es256) signing — when each? rbac vs abac — what's the difference? what is object-level authorization and why is it the one that bites? what is function-level authorization (and bfla)? how do you keep authorization consistent across dozens of microservices? what is the #1 risk on the owasp api top 10 (2023)? walk through a bola on get /accounts/{id} and the fix. what is bopla (api3) and a concrete example? what is broken authentication (api2)? what is unrestricted resource consumption (api4) and how do you defend it? what is ssrf (api7) in an api context? why did the 2023 owasp api list elevate authorization risks to the top? why is tls non-negotiable for token-based auth? what do the httponly , secure , and samesite cookie flags do? xss vs csrf — which cookie flag addresses which, and what's the residual risk? what does least-privilege scoping look like in practice? design auth for a multi-tenant saas used by end users and partner backends. concretely, how do you prevent bola on /accounts/{id} in a multi-tenant app? a token has leaked. walk through your response. public api: api keys or oauth2? design single sign-on (sso) across several of your own apps. an interviewer says \"we'll just use unguessable uuids instead of authorization checks.\" respond. how do you propagate end-user identity through a chain of microservices safely? design end-to-end authentication and authorization for a multi-tenant saas api serving both end users and partner backends. design the authorization model and owasp-api-top-10 defenses for a public rest api exposing per-user resources by id."},{"t":"API Errors, Rate Limiting & Observability Questions","u":"rest-api/interview/0009-error-design-rate-limiting-observability.html","g":"REST API","k":"Interview","x":"api errors, rate limiting & observability questions practice error design, rate limiting and observability with 48 senior and staff rest api interview questions on problem+json, 429s, retries, logs, and tracing. errors, rate limiting & observability — interview questions what is the current standard for http api error bodies? what are the standard fields of a problem details object? why is one consistent error contract across the whole api a senior concern? problem details gives you a shape — what's still missing for clients? why must clients branch on error codes, not error prose? why should you never put stack traces or sql in an error response? a team returns 200 ok with {\"success\":false} for errors. what's wrong? how do you roll out a consistent error contract across an existing api with many teams? 400 vs 422 — when each? 401 vs 403 — what's the distinction? 404 vs 409 — when do you reach for each? when is returning 404 instead of 403 the right call? why does consistent status mapping matter beyond aesthetics? a partner's 4xx errors are spiking your 5xx dashboards. what's likely wrong and how do you fix it? why rate-limit an api at all? what's the http contract for a throttled request? name the four common rate-limiting algorithms. fixed window — how it works and its flaw? sliding window — what does it buy you? token bucket — how it works and why it's the common default? leaky bucket — when would you pick it over token bucket? per-principal vs per-ip limiting — what's your default? why separate burst from sustained quotas? how do you enforce a global rate limit across many distributed api nodes? clients hammer you, all retry on 429 at once, and the spike repeats every reset. what's happening? what are the three pillars of observability? why structured (json) logs over free-text logs? what are the red metrics? what is a correlation / request id and why does it matter? why report latency as p95/p99 instead of the mean? what does distributed tracing give you that logs and metrics don't? what is an slo and an error budget? tracing every request is too expensive at scale. how do you sample without going blind? \"we have lots of dashboards but still can't answer new questions.\" what's the gap? what problem do idempotency keys solve here? how does an idempotency key actually work server-side? why backoff with jitter , not just exponential backoff? what does a circuit breaker do, and why? when should a server return 503 with retry-after ? what is graceful degradation and how do you design for it? how do rate limiting, retries, idempotency, and circuit breakers fit together as one story? \"a partner says our api is impossible to integrate against and keeps falling over.\" redesign it. latency just spiked. walk me through debugging it with the three pillars. how would you choose a rate-limit algorithm for a public write-heavy api? what should you log, and what must you never log? you inherit a fragile, hard-to-integrate api. what's your prioritized remediation plan? how do you decide whether a degradation is \"bad enough to page someone\" at 3am? why do interviewers probe errors, rate limiting, and observability together? design the error contract, abuse protection, and observability for a public write-heavy rest api a partner says is \"impossible to integrate against and keeps falling over.\" design the end-to-end resilience and observability story for a payments-style api where retries must never double-charge and outages must degrade gracefully."},{"t":"Async & Long-Running API Operations Interview Questions","u":"rest-api/interview/0010-async-and-long-running-operations.html","g":"REST API","k":"Interview","x":"async & long-running api operations interview questions practice async and long-running operations with 40 senior and staff rest api interview questions on 202 accepted, status polling, callbacks, webhooks, and jobs. async & long-running operations — interview questions when should an api handle work asynchronously instead of in the request? what's wrong with just holding the connection open for a few minutes? what does 202 accepted mean? walk through the async request–reply (202 + polling) pattern. what does the location header point to in a 202 response? what is a webhook, and how does it differ from polling? what delivery guarantee do webhooks give, and what follows from it? anyone can post to a public callback url. how do you trust the payload? polling vs webhooks — the one-line trade-off each. what's the core mental shift when moving from sync to async design? give concrete examples of work that belongs behind an async api. describe google's long-running operation model. how should you control how often clients poll? why keep the operation queryable after it has completed? should the result live in the operation resource or a separate resource? why? webhooks aren't ordered. how do you build a correct consumer anyway? why should a webhook receiver ack fast and process later? what happens when the receiver returns a non- 2xx (or times out)? explain dead-lettering for webhooks. how do you dedupe reliably on the consumer side? why hmac over the raw body specifically, and what's the common pitfall? a valid signed request can be captured and resent. how do you stop replay? how do you rotate a webhook signing secret without dropping events? where do sse, websockets, and long-polling fit? a client is behind a corporate firewall / can't run a public server. which pattern? why do mature apis often offer both a status endpoint and webhooks? design an api for a large data export that may take minutes, then notifies the client. walk me through making a webhook consumer robust. two events for the same object arrive out of order. what goes wrong, and how do you handle it? the same payment-succeeded webhook is delivered twice. how do you avoid double-fulfilment? a sync endpoint occasionally takes 90 seconds. do you just bump the timeout? how do idempotency keys interact with the 202 submission? how would you let a client cancel a long-running operation? \"just give me exactly-once delivery.\" how do you respond? beyond signatures, what else hardens a webhook endpoint? how does pattern choice change as the event rate scales 100×? a customer's webhook endpoint has been down for two hours. what should your platform do? how do you keep one slow or failing consumer from degrading webhook delivery for everyone? design an api for a large data export that can take several minutes, then notifies the client when it's ready. design a webhook delivery platform: how do you guarantee customers eventually get every event without one bad endpoint taking the system down?"},{"t":"REST API System Design Interview Questions","u":"rest-api/interview/0011-mock-interview-whiteboard-design.html","g":"REST API","k":"Interview","x":"rest api system design interview questions practice api system design with 48 senior and staff rest mock interview questions and whiteboard rounds covering end-to-end design, trade-offs, and follow-ups. system design & mixed rounds — interview questions what is a \"design x's api\" round actually testing? what should you do first in a design round, before any endpoint? what's the winning arc of a design answer in one phrase? for any \"design x\" prompt, what two things must every list endpoint have? which write should return 202 instead of 201 ? which http methods are already idempotent, so they don't need an idempotency key? why prefer a structured error format (rfc 9457) over a bare string? what makes an api horizontally scalable in the first place? \"why is object-level authorization so important?\" what's the single biggest mistake candidates make in this round? name the clarifying questions you ask before drawing anything. walk the 11-step framework from memory. how do you drive a whiteboard round — manage time and structure? how do you communicate a trade-off so it reads as \"senior\"? design the api for a url shortener (create, redirect, analytics). design the api for a notifications service (multi-channel send, preferences, delivery status). design a rate-limited public api (developer-facing, plans, quotas). design the api for a job/task queue service (submit work, poll status, get result). rest vs graphql vs grpc — how do you pick for a given need? sync vs async — how do you decide per endpoint? cursor vs offset pagination — defend your default. versioning: uri path vs header vs date-based — which and why? should every endpoint support idempotency keys? \"tell me about an api you designed and a decision you'd redo.\" \"what's the biggest api mistake you've seen or made?\" \"how do you decide a resource model?\" \"how do you ensure api quality across a whole team?\" \"a stakeholder demands a breaking change next sprint. how do you handle it?\" \"how do you balance shipping speed against api design rigor?\" the interviewer twists a constraint mid-answer (\"now there are 10m third-party clients\"). how do you respond? design the api for a hotel / room booking system (search, hold, book, cancel). design the api for a file storage service like dropbox (upload large files, share, list, versions). design the api for a payments / charges service (charge, refund, list). design the api for a chat / messaging service (send, fetch history, real-time delivery). design the api for a ride-hailing booking flow (request ride, match, track, complete). design the api for a multi-tenant saas (tenant isolation, roles, per-tenant config). design the api for e-commerce cart → order → fulfilment. consistency vs availability — how does cap show up in api design? when is it right to break rest conventions? when does hateoas actually earn its cost? \"what changes at 100×?\" — how do you answer this generically? how does caching / cdn change your api design at scale? how do read replicas / sharding leak into the api contract? how do you design rate limiting & abuse protection at scale? what does backward-compatible evolution actually require? how do you deprecate an endpoint or version at scale? design the api for a payments service - charge a card, refund, list transactions. the interviewer twists the constraint mid-answer: \"now there are 10m untrusted third-party clients.\" walk the deltas."},{"t":"What Makes an API RESTful? REST Constraints","u":"rest-api/lessons/0001-what-makes-an-api-restful.html","g":"REST API","k":"Lesson","x":"what makes an api restful? rest constraints what makes an api restful? learn roy fielding's six rest constraints — statelessness, uniform interface, hateoas, cacheability — and how to nail the interview answer. what actually makes an api restful? the six constraints (and the senior \"why\") what \"stateless\" buys you the framing device: richardson maturity model retrieval practice rehearse the answer out loud interview — pick your bar what is rest, precisely? name the six architectural constraints. what is hateoas? describe the richardson maturity model. is rest the same thing as http? why does statelessness help you scale — and what does it not forbid? if hateoas is part of rest, why is it so rare in real apis? rest vs graphql vs grpc — when would you pick each? is a level 2 api \"really rest\" by fielding's own definition — and how do you defend it? when would you actually invest in hateoas / level 3? a team says \"let's make our api restful.\" how do you advise them? design a restful api for a blogging platform (posts, comments, authors). walk the resource model and maturity level. you're handed a level 0 \"json over http\" api (one post /api with an action field) and asked to make it restful. how do you approach it?"},{"t":"HTTP Methods & Status Codes Explained","u":"rest-api/lessons/0002-http-methods-and-status-codes.html","g":"REST API","k":"Lesson","x":"http methods & status codes explained http methods and status codes explained: get, post, put, patch, delete semantics, safe vs idempotent methods, and choosing the right 2xx/4xx/5xx code for rest apis. http methods & status codes, done right three orthogonal properties (don't conflate them) put vs post for creation status codes that separate seniors retrieval practice rehearse the answer out loud primary source (read this next) interview — pick your bar define a safe and an idempotent http method. is post idempotent? why does it matter? 200 vs 201 vs 204 , and what must a 201 include? 401 vs 403 — what's the difference? explain \"safe idempotent,\" and why \"safe means secure\" is false. is patch idempotent, and how do json merge patch vs json patch relate? why prefer 307 / 308 over 302 / 301 , and why is 304 a feature? when should a forbidden resource return 404 instead of 403 ? a teammate puts \"mark as read\" behind get /messages/42?read=true . what breaks? a downstream dependency is timing out under load. walk the status codes and behavior you'd return. status code as the contract, or body error codes — how do you reconcile them? design the request/response contract for post /payments where the connection can drop mid-flight. walk the methods and status codes. design the update + concurrency story for a resource many clients edit at once (put vs patch, conditional requests)."},{"t":"API Idempotency & Safe Retries","u":"rest-api/lessons/0003-idempotency-in-practice.html","g":"REST API","k":"Lesson","x":"api idempotency & safe retries api idempotency explained: how idempotency keys make post retries safe, dedupe duplicate requests, and survive network failures without double-charging in rest apis. idempotency in practice what \"idempotent\" actually means the idempotency-key pattern implement it like a senior idempotency ≠ exactly-once retrieval practice rehearse the answer out loud primary source interview — pick your bar what does it mean for an operation to be idempotent? how do you make a non-idempotent post safe to retry? who generates the idempotency key, and why? should you apply idempotency keys to every endpoint? is \"idempotent\" the same as \"returns the same response\"? two requests arrive concurrently with the same key — what happens? why fold the key-write into the same transaction as the effect? does idempotency give you exactly-once delivery? should the idempotency layer live in the gateway or the service? the process crashes after the effect commits but before responding. what does the next retry see? what goes wrong if you cache the response but the original operation actually failed? design post /orders so a flaky client can retry without creating duplicate orders. design a message/event consumer that must not double-process a payment when the broker delivers at-least-once."},{"t":"REST Resource Modeling & URI Design","u":"rest-api/lessons/0004-resource-modeling-and-uri-design.html","g":"REST API","k":"Lesson","x":"rest resource modeling & uri design rest resource modeling and uri design: nouns over verbs, collections vs sub-resources, nesting depth, naming conventions, and clean url patterns for scalable apis. resource modeling & uri design the trap: verbs in the path modeling actions that aren't crud nesting: containment, but shallow identifiers, relationships, conventions retrieval practice rehearse the answer out loud primary source (read this next) interview — pick your bar why is post /getorder an anti-pattern? what is the difference between a collection uri and an item uri? what does nesting like /customers/1/orders communicate? why prefer opaque identifiers over sequential integers in uris? why does the noun/verb discipline matter beyond aesthetics? what are the three principled ways to model a non-crud action? why is deep nesting like /customers/1/orders/42/items/9 fragile? what is a bola / idor vulnerability and how does id choice relate to it? articulate the pure-rest vs pragmatist tension on custom methods. what are the downsides of a flexible ?expand= / field-selection mechanism? how does uri shape interact with object-level authorization? model an e-commerce checkout (cart, place order, cancel, refund) on a whiteboard. design the resource model for a multi-tenant project-management api with users, projects, tasks, comments, and memberships."},{"t":"REST API Versioning Strategies","u":"rest-api/lessons/0005-api-versioning-strategies.html","g":"REST API","k":"Lesson","x":"rest api versioning strategies rest api versioning strategies compared: uri path /v1 vs header and media-type versioning, breaking vs non-breaking changes, deprecation, and evolving apis safely. versioning & api evolution the line that decides everything where the version lives: three strategies how the exemplars actually do it deprecation is a lifecycle, not a delete retrieval practice rehearse the answer out loud primary source (read this next) interview — pick your bar what is a breaking change? what is a \"tolerant reader\"? what are the three places to put a version? what does the sunset header do? \"how do you version a public api?\" what's the senior reframe? why is \"just cut a new version\" the wrong default? why is per-account pinning (stripe) such a strong pattern? how do you decide when it's safe to actually remove the old version? a field is technically optional but every client has always sent it. you want to drop it from responses. breaking? how do you coordinate a migration across clients you don't control? how do you keep the support cost of multiple live versions from compounding? design a versioning & deprecation strategy for a public payments api with thousands of integrators. you must make a breaking change to a widely-used response shape — design the rollout so no client breaks."},{"t":"API Pagination at Scale: Cursor vs Offset","u":"rest-api/lessons/0006-pagination-at-scale.html","g":"REST API","k":"Lesson","x":"api pagination at scale: cursor vs offset api pagination at scale: why offset pagination breaks on large changing datasets, how cursor and keyset pagination work, and stable page design for rest apis. pagination at scale offset is not wrong — it's narrow failure 1 · cost grows with depth failure 2 · the window drifts under writes the senior answer: cursor / keyset the count nobody warns you about how the exemplars actually do it retrieval practice rehearse the answer out loud primary source (read this next) interview — pick your bar what's the single biggest performance problem with deep offsets? what is cursor (keyset) pagination, in one sentence? why does cursor pagination's cost stay flat with depth? why hand the client an opaque cursor instead of raw column values? explain the consistency problem with offset pagination under concurrent writes. write the core keyset query and explain each piece. the product wants a total count on a huge feed. what are your options? what does cursor pagination cost you compared to offset? the tuple comparison (a,b) > (x,y) — does every database optimize it as a single index range seek? how do filtering and sorting interact with cursor pagination? you inherit an offset api in production and need to migrate to cursors without breaking clients. how? design pagination for an activity feed with millions of rows and constant inserts. design a paginated, filterable, sortable list endpoint that must also support jump-to-page in an admin ui."},{"t":"HTTP Caching & Conditional Requests (ETags)","u":"rest-api/lessons/0007-caching-and-conditional-requests.html","g":"REST API","k":"Lesson","x":"http caching & conditional requests (etags) http caching for rest apis: cache-control directives, etags and last-modified, conditional get with if-none-match, 304 responses, and optimistic concurrency control. caching & conditional requests freshness — the cache-control directives map validation — the if-none-match → 304 round-trip the fresh → stale → revalidate lifecycle conditional writes — optimistic concurrency caching layers & the vary trap retrieval practice rehearse the answer out loud primary source (read this next) interview — pick your bar what are the two distinct halves of http caching? what's the difference between public and private ? walk through a conditional get with if-none-match . what status comes back if nothing changed? what status code signals a failed precondition, and what does the client do? no-store vs no-cache — what's the difference? (most candidates trip here.) how does if-match prevent lost updates, end to end? strong vs weak etags ( \"abc\" vs w/\"abc\" ) — what's the distinction and when do you use each? what is the cache key by default, and what does vary change? how would you generate etags, and what's the cost trade-off? same etag drives caching and concurrency — articulate why that's elegant, and any tension. a write happens at the origin but a cdn still serves the old copy. what's going on and how do you fix it? design the caching & conditional-request strategy for a read-heavy public product catalog api fronted by a cdn. design optimistic-concurrency control for a collaborative document api where many clients edit the same resources."},{"t":"API Authentication & Authorization (OAuth2/JWT)","u":"rest-api/lessons/0008-authentication-and-authorization.html","g":"REST API","k":"Lesson","x":"api authentication & authorization (oauth2/jwt) api authentication and authorization: authn vs authz, oauth2 flows, jwt vs opaque tokens, scopes and rbac, and securing rest endpoints against common attacks. authentication & authorization the distinction everything hangs on credential schemes and where each fits the oauth2 authorization-code flow (+ pkce) jwt structure — and the trade-off vs. sessions the model that actually gets you breached transport & hygiene (forgotten under pressure) retrieval practice rehearse the answer out loud primary source interview — pick your bar define authentication vs authorization. which status code maps to a missing/invalid credential vs an authenticated-but-not-permitted request? is oauth2 an authentication protocol? what is the #1 risk on the owasp api top 10 (2023)? what problem does pkce solve, and how? what is the aud (audience) claim and why must a resource server check it? what is object-level authorization and why is it the one that bites? what is the chief weakness of a jwt versus a server-side session? explain the alg: none and algorithm-confusion attacks. mtls vs signed jwts for service-to-service auth — how do you choose? how do you keep authorization consistent across dozens of microservices? design end-to-end authentication and authorization for a multi-tenant saas api serving both end users and partner backends. design the authorization model and owasp-api-top-10 defenses for a public rest api exposing per-user resources by id."},{"t":"API Error Design, Rate Limiting & Observability","u":"rest-api/lessons/0009-error-design-rate-limiting-observability.html","g":"REST API","k":"Lesson","x":"api error design, rate limiting & observability api error design, rate limiting, and observability: consistent error contracts (rfc 7807), 429 with retry-after, token-bucket throttling, logs, metrics, and tracing. errors, rate limiting & observability error design: one machine-readable contract rate limiting: protect availability and cost observability: see what actually broke retrieval practice rehearse the answer out loud primary source (read this next) interview — pick your bar what is the current standard for http api error bodies? 400 vs 422 — when each? what's the http contract for a throttled request? what are the red metrics? why must clients branch on error codes, not error prose? why does consistent status mapping matter beyond aesthetics? token bucket — how it works and why it's the common default? why report latency as p95/p99 instead of the mean? how do you enforce a global rate limit across many distributed api nodes? how do rate limiting, retries, idempotency, and circuit breakers fit together as one story? tracing every request is too expensive at scale. how do you sample without going blind? design the error contract, abuse protection, and observability for a public write-heavy rest api a partner says is \"impossible to integrate against and keeps falling over.\" design the end-to-end resilience and observability story for a payments-style api where retries must never double-charge and outages must degrade gracefully."},{"t":"Async & Long-Running REST Operations","u":"rest-api/lessons/0010-async-and-long-running-operations.html","g":"REST API","k":"Lesson","x":"async & long-running rest operations async and long-running rest operations: the 202 accepted pattern, status polling, job resources, webhooks vs polling, and designing apis for exports and batch jobs. async & long-running operations when synchronous stops fitting the job as a state machine polling vs. webhook callback webhook delivery semantics retrieval practice rehearse the answer out loud primary source (read this next) interview — pick your bar what does 202 accepted mean? what does the location header point to in a 202 response? what is a webhook, and how does it differ from polling? what delivery guarantee do webhooks give, and what follows from it? what's the core mental shift when moving from sync to async design? why keep the operation queryable after it has completed? webhooks aren't ordered. how do you build a correct consumer anyway? a valid signed request can be captured and resent. how do you stop replay? \"just give me exactly-once delivery.\" how do you respond? how do idempotency keys interact with the 202 submission? how do you keep one slow or failing consumer from degrading webhook delivery for everyone? design an api for a large data export that can take several minutes, then notifies the client when it's ready. design a webhook delivery platform that guarantees every event eventually lands without one bad endpoint taking the system down."},{"t":"REST API Design Mock Interview","u":"rest-api/lessons/0011-mock-interview-whiteboard-design.html","g":"REST API","k":"Lesson","x":"rest api design mock interview a rest api design mock interview walkthrough: scope the problem, model resources and uris, pick status codes, and handle auth, pagination, idempotency, and errors live. mock interview: design an api the senior api-design framework what separates junior from senior worked example — a payments service api now you — three prompts to rehearse prompt 1 — url shortener prompt 2 — hotel / room booking prompt 3 — file storage (dropbox-like) primary source (read this next) interview — pick your bar what is a \"design x's api\" round actually testing? what should you do first in a design round, before any endpoint? what's the winning arc of a design answer in one phrase? for any \"design x\" prompt, what two things must every list endpoint have? what's the single biggest mistake candidates make in this round? walk the 11-step framework from memory. how do you communicate a trade-off so it reads as \"senior\"? cursor vs offset pagination — defend your default. the interviewer twists a constraint mid-answer (\"now there are 10m third-party clients\"). how do you respond? consistency vs availability — how does cap show up in api design? \"what changes at 100×?\" — how do you answer this generically? design the api for a payments service - charge a card, refund, list transactions. design the api for a hotel / room booking system - search, hold, book, cancel."},{"t":"API Design Interview Framework","u":"rest-api/reference/api-design-interview-framework.html","g":"REST API","k":"Reference","x":"api design interview framework a senior api design interview framework: a step-by-step method for rest design rounds covering requirements, resources, contracts, trade-offs, and follow-ups. senior api design interview framework the 11-step framework method quick-pick status quick-pick phrases that signal senior "},{"t":"API Security & Auth Cheat Sheet","u":"rest-api/reference/api-security-and-auth.html","g":"REST API","k":"Reference","x":"api security & auth cheat sheet an api security cheat sheet covering authentication, authorization, oauth2, jwt, scopes, and rate limiting — a fast reference for rest api security interviews. api security, auth & rate limiting authn vs authz credential schemes oauth 2.0 grant types jwt vs server-side sessions authorization models owasp api security top 10 (2023) rate limiting hygiene checklist "},{"t":"Caching, Pagination & Versioning Cheat Sheet","u":"rest-api/reference/caching-pagination-versioning.html","g":"REST API","k":"Reference","x":"caching, pagination & versioning cheat sheet a rest cheat sheet on http caching with etags, cursor and offset pagination, and api versioning strategies — a fast reference for senior api design interviews. caching, pagination & versioning caching & conditional requests pagination versioning & evolution "},{"t":"Idempotency & Retries Cheat Sheet","u":"rest-api/reference/idempotency-and-retries.html","g":"REST API","k":"Reference","x":"idempotency & retries cheat sheet an idempotency and retries cheat sheet covering idempotency keys, safe retries, exactly-once semantics, and dedup — a quick reference for rest api interviews. idempotency & retries quick definitions method idempotency at a glance the idempotency-key pattern implementation checklist idempotency ≠ exactly-once retry strategy "},{"t":"REST Constraints & Maturity — Cheat Sheet","u":"rest-api/reference/rest-constraints-and-maturity.html","g":"REST API","k":"Reference","x":"rest constraints & maturity — cheat sheet a rest cheat sheet on the six architectural constraints, the richardson maturity model, hateoas, and http method semantics — quick reference for api interviews. constraints, maturity & method semantics the six architectural constraints richardson maturity model http method semantics status code families "},{"t":"LSM-Tree Compaction Explained","u":"storage-engines/lsm-compaction-deep-dive.html","g":"Storage Engines","k":"Lesson","x":"lsm-tree compaction explained lsm-tree compaction in depth: leveled vs tiered strategies, the write/read/space amplification trade-off, and the tombstone resurrection bug — pick per table. lsm compaction — the trade-off you cannot escape the job & the three amplifications size-tiered (stcs) — the write-optimized corner leveled (lcs) — the read-and-space-optimized corner the trade-off triangle — pick two of three tombstones & the resurrection bug l0, write stalls & back-pressure interview — pick your bar retrieval practice — mix the levels reconstruct the deep dive from a blank page ★ cheat sheet — compaction in one screen strategy → workload numbers to remember three rules for the senior chair memory check memory check memory check memory check memory check memory check walk the lsm write path from a client write to a key on disk: memtable, wal, sstable. what is an sstable, and why does its sortedness make merging cheap? name the three amplifications and say who pays for each. what does a bloom filter do in an lsm read path, and what guarantee does it give? compare size-tiered and leveled compaction. when do you pick each? why can leveled compaction guarantee a point read touches at most one sstable per level? walk me through the tombstone resurrection bug and how engines prevent it. what triggers compaction, and why does an lsm under heavy ingest start stalling writes? should one compaction strategy apply to the whole database? make the principal-level call. \"leveled is the modern default, just use it everywhere.\" steelman it, then take it apart. where do twcs and rocksdb's universal compaction sit relative to stcs and lcs, and why do hybrids exist? design the compaction strategy for a write-heavy ingest table vs a read-heavy lookup table in the same cluster. design deletes + compaction for a cassandra time-series store that must honor data-retention (ttl) and gdpr-style erasure."},{"t":"Storage Engines: B-Trees vs LSM-Trees","u":"storage-engines/storage-engines-fundamentals.html","g":"Storage Engines","k":"Lesson","x":"storage engines: b-trees vs lsm-trees how a database puts bytes on disk and finds them again — b-trees vs lsm-trees, indexes, and data models — so you can choose a storage engine with real judgment. storage engines from first principles log + hash index — the simplest real engine b-tree — the read-optimized default lsm-tree — write-optimized, log all the way down amplification — the three currencies you trade indexes — clustered vs secondary, and the write tax data models — relational, document, graph normalization vs denormalization — where each fact lives encoding & evolution — bytes that outlive their writer row vs column — match the layout to the workload where each layer shows up — real engines retrieval practice — mix the levels reconstruct the engine from a blank page interview — pick your bar ★ cheat sheet — name → mechanism translation table three rules for the architect chair ★ review guides & what to read next memory check memory check memory check memory check memory check memory check memory check memory check memory check walk me through what a storage engine does to turn \"find me row 42\" into disk i/o. what is a clustered index versus a secondary index, and where does the row actually live? what is a composite index on (a, b) , and which queries can use it? what is a write-ahead log (wal), and why does a b-tree need one? a teammate says \"lsm-trees are just faster than b-trees.\" correct them. define read, write, and space amplification, and explain why you can't drive all three to zero. an innodb secondary-index lookup is slow. explain the mechanics and a fix. what does the page / buffer cache do, and how does it change how you reason about read cost? how do you choose between a b-tree (row store) and an lsm-tree for a new service, and how would you defend reversing that choice later? \"add an index, the query's slow\" — make the principal-level case for when not to add one. when is a column store the right call over a row store, and what exactly are you trading? design the storage for a product with live order pages and an analytics dashboard. design the storage engine + indexing for a write-heavy event-ingest service that also needs fast recent-event reads."},{"t":"Exactly-Once Semantics in Kafka","u":"streaming-event-driven/exactly-once-kafka-deep-dive.html","g":"Streaming & Event-Driven","k":"Lesson","x":"exactly-once semantics in kafka exactly-once semantics in kafka explained: idempotent producers, transactions, and committing consumer offsets inside the producer transaction to make read-process-write loops exactly-once. exactly-once in kafka — deep dive what eos actually promises (and doesn't) the idempotent producer — pid + sequence numbers transactions — the loop made atomic read_committed and the last stable offset zombie fencing — the transactional.id and the epoch the boundary — where kafka's exactly-once stops where each mechanism shows up — and what it buys retrieval practice — mix the mechanisms reconstruct the guarantee from a blank page interview — pick your bar ★ cheat sheet — claim → what it actually means translation table three rules for the design review ★ review guides & what to read next memory check memory check memory check memory check memory check memory check \"exactly-once in kafka\" — exactly-once what ? define the scope precisely. what does the idempotent producer do, and what does it require? what is read_committed , and why must every consumer set it for eos? where are consumer offsets stored, and why does that matter for transactions? walk me through how a per-partition sequence number dedupes a producer retry. why does committing the consumer's input offset inside the producer's transaction make the loop exactly-once? i produce transactionally but downstream still sees duplicates. diagnose it. what's the lso, and what does read_committed cost you? what is a \"zombie\" producer, and how does kafka stop it without solving failure detection? your processing also writes to a sql db and charges a card. how far does kafka eos get you? distinguish the two failure modes — a duplicate and a gap — in the eos machinery. design an exactly-once enrichment job (kafka in, enrich, kafka out). design an exactly-once pipeline whose last step charges a payment provider."},{"t":"Streaming & Event-Driven Architecture","u":"streaming-event-driven/streaming-event-driven-fundamentals.html","g":"Streaming & Event-Driven","k":"Lesson","x":"streaming & event-driven architecture streaming and event-driven architecture from first principles: logs, topics, partitions, delivery guarantees, event time vs processing time, and how to design sound async pipelines. streaming & event-driven, from first principles the log — the one structure under everything kafka — one log becomes many delivery guarantees — one choice decides everything events vs commands vs state — what goes on the topic event sourcing — the log is the state cqrs — one write model, many read models stream processing — computation on the moving log time, windows & watermarks — \"the last 5 minutes\" of what clock? building it right — the capstone checklist where each layer shows up — real systems interview — pick your bar retrieval practice — mix the levels reconstruct the stack from a blank page ★ cheat sheet — buzzword → engineering translation table three rules for the review chair ★ review guides & what to read next memory check memory check memory check memory check memory check memory check memory check memory check memory check what is the difference between a log and a message queue? what does the offset of a record identify, and to what scope does kafka guarantee ordering? define at-most-once, at-least-once, and exactly-once delivery in one line each. what is an event vs a command, and how does that shape coupling? a consumer reads, processes, then commits the offset. walk me through the failure modes. someone says \"we use kafka exactly-once, so duplicates are impossible.\" push back. explain cqrs and the consistency trade-off you're signing up for. what is backpressure in a streaming pipeline, and how does kafka's model handle it? when is event sourcing the right call, and what does it cost you? event time vs processing time — why do they diverge, and how do you decide a window is \"done\"? how does a stateful stream operator survive a crash without recomputing from the beginning? design a reliable order-events pipeline that is safe to operate at 3 a.m. design a read-model / cqrs layer that can be rebuilt at will."},{"t":"Watermarks & Event-Time Windowing","u":"streaming-event-driven/watermarks-deep-dive.html","g":"Streaming & Event-Driven","k":"Lesson","x":"watermarks & event-time windowing watermarks and event-time windowing explained: how a watermark estimates completeness, advances by the min rule, triggers windows, handles late data, and why an idle input stalls a pipeline. watermarks — the completeness clock of streaming what a watermark actually asserts perfect vs heuristic — the tension that defines streaming the min rule — and the idle-partition stall triggers — when to fire is not the watermark allowed lateness — and the three-way trade-off same primitives, opposite dials — by workload retrieval practice — mix the levels reconstruct the topic from a blank page interview — pick your bar ★ cheat sheet — term → what it actually is translation table three rules for the on-call chair ★ review guide & what to read next memory check memory check memory check memory check memory check event time vs processing time — define each and why they diverge. define a watermark precisely, and say why it's like a timeout. name the window types — tumbling, sliding, session — and what each is for. what is allowed lateness, and what happens to data past it? perfect vs heuristic watermark — when can you have a perfect one, and what does it buy? your pipeline is processing records but emitting nothing, with no error. diagnose. why is the propagation rule a min and not a max or average? \"but what about late data?\" — answer it without dropping anything. separate windowing, triggers, and accumulation. where does the watermark fit? argue that there is no \"correct\" watermark setting. how do triggers, allowed lateness, and accumulation mode interact to give correct-but-eventually-consistent output? design event-time windowing for an out-of-order stream (e.g. mobile clients with intermittent connectivity). design a monthly billing rollup vs a real-time fraud signal on the same event stream."},{"t":"MVCC Internals Explained","u":"transactions-isolation/mvcc-internals-deep-dive.html","g":"Transactions & Isolation","k":"Lesson","x":"mvcc internals explained mvcc internals: how databases store row version chains, how a snapshot decides which version it sees, and why one long-running transaction causes table bloat and vacuum pressure. mvcc internals — version chains, visibility & the bloat trap two ways to store versions — heap vs undo visibility rules — how a snapshot decides vacuum, purge & the long-transaction bloat trap the transaction-id wraparound time bomb loose ends — indexes, ssi & which model wins where each piece bites — production map retrieval practice — answer from memory reconstruct the deep dive from a blank page interview — pick your bar ★ cheat sheet — internal → what it means translation table three rules for the on-call chair ★ review guide & what to read next memory check memory check memory check memory check memory check what does multi-version storage mean — what happens to old data when a row is updated? what exactly is a snapshot, and what is in it? state the visibility rule: how does a reader decide a given version is visible? what is version gc — vacuum in postgres, purge in innodb — and why is it needed? walk me through exactly what happens on disk when you update one row in postgres vs innodb. a table is 5 the size of its live row count and seq scans are slow. diagnose it. why is updating an indexed column more expensive under mvcc — in both engines? connect a long-running analytics query to table bloat, and give the operational fix. \"the database suddenly stopped accepting writes.\" what's your first hypothesis and how does it tie to mvcc? postgres heap-history vs innodb undo-history — how does that one design choice ripple through operations? how is postgresql serializable built on top of mvcc, and what does it add — and cost? design the version-storage and gc strategy for an append-mostly audit-log table that is rarely updated but read by long analytics scans. you're choosing between a postgres-style (heap history) and an innodb-style (undo history) engine for a write-heavy, high-contention oltp service. walk the decision."},{"t":"Serializable Snapshot Isolation (SSI)","u":"transactions-isolation/ssi-deep-dive.html","g":"Transactions & Isolation","k":"Lesson","x":"serializable snapshot isolation (ssi) serializable snapshot isolation (ssi) explained: how a database keeps non-blocking snapshot reads yet stays serializable by tracking rw-dependencies and aborting dangerous write skew. serializable snapshot isolation snapshot isolation's blind spot — write skew serializability is a graph property the dangerous structure — two rw-edges and a pivot how postgresql catches it — sireads & a conservative abort the price, the tuning, and the escape hatch interview — pick your bar retrieval practice — mix the levels reconstruct the argument from a blank page ★ cheat sheet — term → engineering translation table three rules for the principal chair memory check memory check memory check memory check memory check snapshot isolation prevents dirty reads and lost updates. so what does it not prevent, and why? what is serializable snapshot isolation, in one breath? what is an rw-antidependency, and why is it the only edge si can't prevent? define serializability precisely enough that you could build a detector from the definition. state fekete's theorem and explain why it makes ssi cheap. why does ssi abort transactions that were actually serializable — and is that a bug? how does postgresql detect an rw-edge at runtime without making reads block, and what must the app do? a serializable workload is aborting too often under load. walk me through your tuning levers. compare si, ssi, and 2pl on what they guarantee and how they fail — and when you'd pick each. your fleet is sharded and each postgres node runs serializable. is the system serializable? a team wants to default the whole oltp service to serializable to \"never think about anomalies again.\" make the principal call. choose a concurrency-control design for a hospital on-call scheduler that must always keep at least one doctor on call. design isolation for a service that runs short invariant-critical writes and long analytical reads against the same database, choosing among si, ssi, and 2pl."},{"t":"Transaction Isolation Levels & Anomalies","u":"transactions-isolation/transactions-isolation-fundamentals.html","g":"Transactions & Isolation","k":"Lesson","x":"transaction isolation levels & anomalies transaction isolation levels explained: read committed, repeatable read, snapshot, serializable — and the anomalies (dirty reads, write skew, phantoms) each one allows or prevents. transactions & isolation — fundamentals acid — the four promises (and what they don't mean) the anomalies — the catalog of concurrency bugs isolation levels — the menu, and why the names lie snapshot isolation & mvcc — how read committed / si actually work lost updates & write skew — the subtle invariant-breakers serializability — serial · 2pl · ssi distributed transactions & 2pc — why two-phase commit blocks sagas, outbox & idempotency — eventual consistency for workflows choosing isolation in practice — the senior decision tree retrieval practice — mix the levels reconstruct the ladder from a blank page interview — pick your bar ★ cheat sheet — symptom → fix anomaly → fix table three rules for the review chair ★ review guides & what to read next memory check memory check memory check memory check memory check memory check memory check memory check memory check what does acid actually promise — and what does \"atomic\" not mean? name the standard isolation levels from weakest to strongest, and what each adds. list the read/write anomalies and give a one-line definition of each. explain how an mvcc engine serves a consistent read without taking locks. two services both set \"repeatable read.\" do they give the same guarantees? a counter is losing increments under load. walk me through diagnosis and the fix ladder. why doesn't snapshot isolation prevent write skew, and how do you actually fix it? locking (2pl) vs mvcc for isolation — what's the real trade-off? \"just set everything to serializable and stop worrying about anomalies.\" argue for, then against. you inherit a service on read committed with occasional \"impossible\" data states. how do you find and classify the bug? an invariant must hold across two microservices with separate databases. how do you reason about isolation here? design the transaction & isolation strategy for a ticket-booking system where each seat may be sold once. choose isolation levels for a system with a high-volume write path and a separate analytics reporting path on the same database."},{"t":"Agent Systems Engineering — How Production AI Executes Work","u":"ai-agents/lessons/0005-agent-systems-engineering.html","g":"AI Agents","k":"Lesson","x":"agent systems engineering how production ai executes work planning task decomposition goal breakdown hierarchical dynamic re-planning react plan-and-execute state management stateless stateful agent context window execution engine sequential parallel branching workflow engine queue system distributed job durable execution loop control observe think act repeat stop conditions success detection infinite loop prevention retries human in the loop hitl approval workflow blast radius autonomy supervised failure recovery retry fallback compensation saga escalation circuit breaker dead letter orchestration single agent coordinator worker router delegation routing ownership long running tasks checkpointing persistence resume recovery cost engineering tokens budget runaway agent cost explosion observability tracing run history tool logs reasoning history audit trail production architecture review design patterns planner executor supervisor approval retriever research anti-patterns guardrails evaluation"},{"t":"AI Evaluation Fundamentals: Offline & Online","u":"ai-evaluation/lessons/0001-evaluation-fundamentals-offline-online.html","g":"AI Evaluation","k":"Lesson","x":"engineering vault prev next ai evaluation lesson 1 · ai evaluation · foundation evaluation fundamentals: offline & online ~14 min · 3 modules · why measurement is the moat, the offline/online split, and the loop that gates every ship offline eval online eval llm-as-judge golden sets eval loop your bar: explain why evaluation — not the model — is the real moat, draw the line between offline eval (a fixed dataset, repeatable, run before you ship) and online eval (live traffic, a/b and shadow, measured after you ship), and walk the loop that turns a production failure into a permanent regression test. by the end you can answer the question every ai team eventually faces: how do you know a change made the system better instead of just different? 1 the whole discipline reduces to four moves. each chip is one of them: you can't improve what you can't measure score offline against a fixed golden set before you ship score online on live traffic after you ship feed every failure back into the golden set 1 · collect golden cases 2 · score prog / human / judge 3 · diff vs baseline 4 · ship / roll back decide on the number 5 · feed failures back into the golden set — the loop closes offline — before you ship fixed dataset · repeatable · runs in ci · gates the merge same inputs every run online — after you ship live traffic · a/b + shadow real distribution the model is a commodity. the dataset of what 'good' means for your task is the moat. offline tells you it's safe to ship. online tells you it actually worked. you cannot improve what you cannot measure. the eval loop in one frame. collect cases, score them, diff against a baseline, ship or roll back , and feed failures back . offline runs the loop on a fixed dataset before ship; online runs it on live traffic after. 1 1 · why evaluation is the moat 2 · offline evaluation (pre-ship) 3 · online evaluation (post-ship) ★ retrieval practice ★ interview questions ★ the loop closes 1 why evaluation is the moat the model is a commodity — your competitor can call the same api. what you can't copy is a curated dataset of what good means for your task and a measured baseline of how well you hit it. you cannot improve what you cannot measur"},{"t":"Reviewing an AI Feature Like a Distributed System","u":"production-ai-architecture/lessons/0001-reviewing-ai-like-a-distributed-system.html","g":"Production AI Architecture","k":"Lesson","x":"engineering vault prev next production ai architecture lesson 1 · production ai architecture · senior reviewing an ai feature like a distributed system ~14 min · 3 modules · the repeatable review that turns \"we added ai\" into a design you can defend reliability scalability observability cost control failure modes your bar: walk into a design review for an ai feature and run it through the same lenses you'd run on any production service — reliability, scalability, availability, observability, cost, failure modes — then name the one new variable ai adds, non-determinism , and show exactly how it bends each lens. by the end you can answer the question a vp actually asks: what happens when the model is slow, wrong, rate-limited, or down — and can you see it? 1 an ai feature is not a new kind of system. it's a service calling a remote dependency. four moves frame the review: it's a distributed system — run the usual lenses the dependency is non-deterministic — the one new variable capacity is a token budget , not rps reliability now includes \"correct-enough,\" not just \"200 ok\" your service request in model call remote · rate-limited answer out maybe wrong reliability correct-enough, not just up scalability tokens/sec vs a provider limit observability + a quality axis: was it right? + one new variable: non-determinism same skeleton as any service you've shipped. the model is just a flaky, remote, non-deterministic dependency. reliability theory isn't reinvented — it's inherited from sre and distributed systems and bent by three deltas. the deltas: correctness is reliability · tokens are capacity · quality is a monitored signal. the whole review in one picture. you already know how to review a service that depends on a remote, rate-limited, occasionally-down api. ai adds exactly one thing — the dependency can be wrong , not just down — and that single fact bends reliability, scalability, and observability. 1 1 · reliability (correct-enough, not just up) 2 · scalability (tokens, not requests) 3 · observability (+ a quality axis) ★ retrieval practice ★ interview questions ★ the vp's questions 1 reliability for a normal service, reliable means \"it responded, within budget.\" for "},{"t":"AI Threat Modeling & Prompt Injection","u":"ai-security/lessons/0001-ai-threat-model-and-prompt-injection.html","g":"AI Security","k":"Lesson","x":"engineering vault prev next ai security lesson 1 · ai security · senior ai threat modeling & prompt injection ~14 min · 3 modules · why the prompt is not a trust boundary, and where the real control lives prompt injection threat model owasp llm tool egress defense in depth your bar: explain why an llm app breaks in ways an ordinary service doesn't — the model reads attacker-controlled text and your instructions in one channel and can't tell them apart. then threat-model the stack: name the assets, the entry points, and the trust boundaries, and say where the control actually belongs once you accept the prompt can't enforce one. 1 the whole lesson reduces to four moves. each chip is one: the model can't separate data from instructions so retrieved text is untrusted data , never commands threat-model: assets, entry points, trust boundaries put the boundary in the query (acl) and tool layer the prompt is not a trust boundary user input direct injection retrieved docs / web indirect injection all attacker-reachable boundary 1 retrieval query · acl llm + context window one channel: data and instructions look identical can't enforce a boundary boundary 2 tools · least priv + egress tools take actions email · db · fetch url outbound = exfil path if egress is open the model is in the blast radius, not on the perimeter. you can't secure it from inside the prompt. two real boundaries: scope what gets retrieved (acl in the query) and what tools can do (least privilege + egress). owasp llm01 prompt injection · root cause: data and instructions share one channel. no layer is complete. the posture is layered controls + least privilege + assume-breach. the hero of the whole track. untrusted text enters from the user and from anything retrieved; the model can't tell it from your instructions; the only enforceable boundaries are the retrieval query (who can see what) and the tool layer (what the agent can do and where it can send). 1 1 · prompt injection (direct & indirect) 2 · threat-modeling an llm app 3 · context poisoning & the real boundary ★ retrieval practice ★ interview questions ★ where the control lives 1 prompt injection — direct & indirect an ordinary app keeps code and dat"},{"t":"The AI Infra Stack End-to-End: Embeddings, Vector DBs & Serving","u":"ai-infrastructure/lessons/0001-the-ai-infra-stack-end-to-end.html","g":"AI Infrastructure","k":"Lesson","x":"engineering vault prev next ai infrastructure lesson 1 · ai infrastructure · core the ai infra stack, end-to-end ~14 min · 3 modules · the map of every hop one query takes from text to answer — and where latency, cost, and failure live embeddings vector dbs rerankers model gateway inference your bar: trace one user query through the whole stack — text -> embedding -> vector db -> reranker -> model gateway -> inference server — and say, at each hop, what it does and where the latency, cost, and failure concentrate. by the end you can answer the foundational interview question: what actually happens to a request between \"user hits enter\" and \"tokens stream back,\" and which hop do you instrument first? 1 the stack is one straight line. every hop transforms the request and hands it on: text becomes a vector (embedding model) the vector db finds near neighbors via ann a reranker sharpens precision on the top-k the gateway and inference server serve the answer one request, every hop query text user input embedding text -> vector vector db ann search hnsw / ivf reranker cross-encoder re-score top-k gateway auth · route cache · limit inference server kv cache · continuous batching gpu forward pass response cache hit -> skip inference tokens stream answer to user where each pain concentrates latency inference forward pass & queue-wait dominate; ann search is fast unless over-tuned cost per-token inference; the gateway is the one place to see and cap total spend failure ann recall gaps · gateway as blast-radius chokepoint · kv memory capping batch size the whole track in one picture. every later lesson zooms into one of these boxes — this lesson is the map: what each hop does , and where latency, cost, and failure live . 1 1 · the stack map (the request path) 2 · embeddings (text -> vector) 3 · vector databases (ann at scale) ★ retrieval practice ★ interview questions ★ where this track goes 1 the stack map the request path is a pipeline of single-purpose hops. each one transforms the request and hands it forward, and each one is where some specific kind of pain — latency, cost, or failure — concentrates. the hop-by-hop request path each hop: one job, one risk embedding model jo"},{"t":"The Domain Agent Design Method: A 9-Part Template","u":"domain-agent-design/lessons/0001-the-domain-agent-design-method.html","g":"Domain Agent Design","k":"Lesson","x":"engineering vault prev next domain agent design lesson 1 · domain agent design · senior the domain agent design method ~13 min · 3 modules · one repeatable 9-part template for designing any business agent, walked once on a worked example responsibilities tool surface approval gates build vs buy roi your bar: stop designing agents ad hoc. take any business domain — billing, support, sre, research — and run it through the same nine-part template, in the same order, every time. by the end you can answer the question an architect actually gets asked: given a domain, what does the agent own, what can it touch, where does a human have to sign off, and is it worth building at all? 1 the later lessons just instantiate this template per domain — this is the method they share. this track turns the archetypes from ai agents · lesson 4 (business agent architecture) into a repeatable design discipline. the whole method is four moves: draw the responsibility boundary before any tool size the tool surface — it is the agent's reach place approval gates by blast radius then run the roi / build-vs-buy test one template · nine parts · same order every domain 1 · responsibilities the boundary 2 · tools the surface 3 · context the window 4 · memory persisted state 5 · runtime the loop 6 · evaluation how you score it 7 · security untrusted input 8 · approval gates by blast radius 9 · roi / build-buy ship or kill boundary (1) sets the surface (2) -> surface sets the risk (6 7) -> risk sets the gates (8) -> value decides ship (9) parts 3 5 are the machinery the agent runs on; 1 2 and 8 9 are the design decisions you own. per-domain lessons (billing, sre, research ) don't reinvent this — they fill it in. the more an agent can do unsupervised, the more an approval gate matters. the method on one page. you walk the nine parts in order because each one constrains the next: the boundary decides the tool surface, the surface decides the blast radius, the blast radius decides where humans sign off, and the roi test decides whether you build it at all. 1 1 · the boundary & the surface (parts 1 2) 2 · the machinery (parts 3 7) 3 · gates & roi (parts 8 9) + worked example retrieval practice interview q"},{"t":"Multi-Agent Systems: When & When Not","u":"advanced-ai-systems/lessons/0001-multi-agent-systems-when-and-when-not.html","g":"Advanced AI Systems","k":"Lesson","x":"engineering vault prev next advanced ai systems lesson 1 · advanced ai systems · staff multi-agent systems: when & when not ~14 min · 3 modules · the skeptical, cost-aware take on coordinating fleets of agents multi-agent orchestration swarms coordination cost anti-patterns your bar: decide whether a problem actually warrants more than one agent, choose orchestrator-worker vs swarm when it does, and name the failure modes that only appear once agents must coordinate. by the end you can answer the production question every pm eventually asks: should this be a fleet of agents, or one well-tooled agent in a loop? 1 the advanced, cost-aware sequel to the ai agents track's single-vs-multi intro — here we add the trade-off math and the anti-patterns. the whole skeptical case fits in four moves. each chip is one of them: default to one well-tooled agent add agents only for genuinely parallel work prefer orchestrator-worker over emergent swarms and pay the coordination tax only when it pays you back a goal one task to do is it parallel? independent sub-work answer cheap + debuggable default · one well-tooled agent all state in one context cheaper, lower-latency, one place to debug only if parallel · a fleet of agents throughput on independent work pays a coordination tax in tokens + latency no -> yes more agents add parallel hands, not a smarter brain. a multi-agent system is the distributed-systems version of an agent — same trade as a monolith vs microservices. don't add agents for the reasons you wouldn't add microservices. the whole lesson in one picture. the single well-tooled agent is the default ; a fleet is a scaling pattern you reach for only when work is genuinely parallel and the coordination tax pays for itself. 1 1 · multi-agent systems (what & when) 2 · orchestrator-worker vs swarm 3 · failure modes & anti-patterns ★ retrieval practice ★ interview questions ★ foundations laid 1 multi-agent systems multiple agents — each its own llm + tools + context window — working on one goal, coordinating by passing messages or results. it's the distributed-systems version of an agent : more throughput on parallel work, paid for with coordination. 2 one agent, or many? start f"}];