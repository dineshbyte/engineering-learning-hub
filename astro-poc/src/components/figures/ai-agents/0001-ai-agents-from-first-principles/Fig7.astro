---
/**
 * ai-agents/0001 · Figure 7 — Level 6, Memory
 * (the runtime refills the bounded context window each call from short-term
 * history and a long-term store).
 * Verbatim from source; shared <marker> inlined + namespaced (-f7). See Fig1.
 */
---

<figure>
    <svg
        viewBox="0 0 520 175"
        role="img"
        aria-label="The runtime refills the bounded context window each call from short-term history and a long-term store"
    >
        <defs>
            <marker
                id="arw-f7"
                viewBox="0 0 10 10"
                refX="8.5"
                refY="5"
                markerWidth="7"
                markerHeight="7"
                orient="auto-start-reverse"
            >
                <path d="M0,0 L10,5 L0,10 z" fill="#6b675f"></path>
            </marker>
        </defs>
        <g font-family="ui-sans-serif,system-ui,sans-serif">
            <rect x="8" y="14" width="360" height="48" rx="9" fill="#fbfaf2" stroke="#7a1f12"
            ></rect>
            <text
                x="188"
                y="33"
                text-anchor="middle"
                font-size="11"
                font-weight="700"
                fill="#7a1f12">CONTEXT WINDOW — RAM (bounded, refilled every call)</text
            >
            <text x="188" y="51" text-anchor="middle" font-size="9.5" fill="#6b675f"
                >system prompt + history so far + retrieved facts</text
            >
            <line x1="372" y1="38" x2="410" y2="38" stroke="#6b675f" marker-end="url(#arw-f7)"
            ></line>
            <rect x="412" y="16" width="96" height="44" rx="9" fill="#f1eee2" stroke="#7a1f12"
            ></rect><text
                x="460"
                y="43"
                text-anchor="middle"
                font-size="13"
                font-weight="700"
                fill="#7a1f12">LLM</text
            >
            <rect x="20" y="104" width="160" height="46" rx="9" fill="#fff" stroke="#d8d4c8"></rect>
            <text x="100" y="124" text-anchor="middle" font-size="10.5" font-weight="700"
                >short-term</text
            ><text x="100" y="139" text-anchor="middle" font-size="9" fill="#6b675f"
                >conversation history</text
            >
            <rect x="200" y="104" width="168" height="46" rx="9" fill="#fff" stroke="#d8d4c8"
            ></rect>
            <text x="284" y="124" text-anchor="middle" font-size="10.5" font-weight="700"
                >long-term · disk</text
            ><text x="284" y="139" text-anchor="middle" font-size="9" fill="#6b675f"
                >DB / vector store (RAG)</text
            >
            <path d="M 100 104 L 130 66" fill="none" stroke="#6b675f" marker-end="url(#arw-f7)"
            ></path>
            <path d="M 284 104 L 250 66" fill="none" stroke="#6b675f" marker-end="url(#arw-f7)"
            ></path>
            <text x="430" y="92" text-anchor="middle" font-size="9.5" fill="#6b675f"
                >runtime refills ↑</text
            >
        </g>
    </svg>
    <figcaption>
        You solved this before: stateless HTTP → sessions + a database. RAG = a <code>SELECT</code> pasted
        into the prompt.
    </figcaption>
</figure>
