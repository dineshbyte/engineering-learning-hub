# build output + caches
dist
.astro
# copied site assets — formatted in their canonical home (docs/assets), not here
public/assets

# generated MDX lessons — bespoke JSX/frontmatter, not Prettier-managed (no MDX plugin)
src/content
src/components/figures
