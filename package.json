{
  "name": "engineering-learning-hub",
  "version": "1.0.0",
  "private": true,
  "description": "Self-paced engineering learning hub — AI Agents, REST, and backend-systems tracks (visual HTML lessons + EPUBs + an in-browser EPUB reader).",
  "//": [
    "WHY THIS FILE EXISTS:",
    "The in-browser EPUB reader (reader.html) loads .epub files with fetch/XHR.",
    "Chromium browsers (Brave, Chrome, Edge) block fetch() of file:// resources for",
    "security, so opening reader.html?book=… straight from disk fails. Serving the",
    "folder over http:// (same-origin) makes those requests legal in every browser.",
    "",
    "HOW TO USE:",
    "  npm start              -> serves this folder, then open http://localhost:8000/",
    "  PORT=3000 npm start    -> custom port",
    "",
    "ZERO RUNTIME DEPS: server.js uses only Node built-ins, and the served site loads",
    "vendored libs from lib/ — so `npm start` and GitHub Pages need no install.",
    "",
    "DEV DEPS (epubjs, jszip) exist ONLY to refresh those vendored files. To update them:",
    "  pnpm install && pnpm run vendor   -> copies dist/ into lib/ (then commit lib/)."
  ],
  "scripts": {
    "start": "node server.js",
    "serve": "node server.js",
    "serve:python": "python3 -m http.server 8000",
    "serve:npx": "npx --yes serve . -l 8000",
    "vendor": "node -e \"const f=require('fs');for(const[s,d]of[['node_modules/jszip/dist/jszip.min.js','lib/jszip.min.js'],['node_modules/epubjs/dist/epub.min.js','lib/epub.min.js']])f.copyFileSync(s,d);console.log('vendored lib/ from node_modules')\""
  },
  "engines": {
    "node": ">=22"
  },
  "keywords": [
    "learning",
    "epub",
    "epub-reader",
    "static-server",
    "ai-agents",
    "rest-api",
    "distributed-systems",
    "system-design"
  ],
  "license": "MIT",
  "devDependencies": {
    "epubjs": "^0.3.93",
    "jszip": "3.10.1"
  }
}
